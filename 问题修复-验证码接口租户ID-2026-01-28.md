# 问题修复：验证码接口不应传递租户 ID 和 Token

**日期**: 2026-01-28  
**问题**: 验证码接口报错 403 - "您无权访问该租户的数据"  
**状态**: ✅ 已修复

---

## 一、问题描述

### 错误信息
```json
{
  "code": 403,
  "data": null,
  "msg": "您无权访问该租户的数据"
}
```

### 问题接口
```
GET http://localhost:48080/admin-api/system/captcha/get
```

### 问题原因
1. 验证码接口在**登录前**调用，此时用户还未登录
2. 没有 Token（Authorization）
3. 没有租户信息（tenant-id）
4. 但是移动端的请求拦截器会自动添加这些信息（如果存在），导致后端验证失败

---

## 二、修复方案

### 方案：将验证码接口加入白名单

将验证码接口同时加入 **Token 白名单** 和 **租户白名单**：

#### 修改前
```typescript
// 请求白名单，无须 token 的接口
const whiteList: string[] = [
	'/login', 
	'/refresh-token', 
	'/system/tenant/get-id-by-name'
]

// 租户白名单，无须 tenant-id 的接口
const tenantWhiteList: string[] = [
	'/system/captcha/',  // ❌ 只在租户白名单中
	'/login',
	'/refresh-token',
	'/system/tenant/get-id-by-name'
]
```

#### 修改后
```typescript
// 请求白名单，无须 token 的接口
const whiteList: string[] = [
	'/system/captcha/',              // ✅ 验证码相关接口
	'/login',                        // 登录接口
	'/refresh-token',                // 刷新 Token 接口
	'/system/tenant/get-id-by-name'  // 获取租户 ID 接口
]

// 租户白名单，无须 tenant-id 的接口
const tenantWhiteList: string[] = [
	'/system/captcha/',              // ✅ 验证码相关接口
	'/login',                        // 登录接口
	'/refresh-token',                // 刷新 Token 接口
	'/system/tenant/get-id-by-name'  // 获取租户 ID 接口
]
```

---

## 三、白名单说明

### Token 白名单（无需 Authorization）

**应该加入的接口**：
1. `/system/captcha/` - 验证码相关接口（登录前调用，无 Token）
2. `/login` - 登录接口（登录前调用，无 Token）
3. `/refresh-token` - Token 刷新接口（Token 过期时调用）
4. `/system/tenant/get-id-by-name` - 租户查询接口（登录前调用）

### 租户白名单（无需 tenant-id）

**应该加入的接口**：
1. `/system/captcha/` - 验证码相关接口（登录前调用，无租户信息）
2. `/login` - 登录接口（登录前调用，无租户信息）
3. `/refresh-token` - Token 刷新接口
4. `/system/tenant/get-id-by-name` - 租户查询接口（用于获取租户信息）

### 规律总结

**登录前调用的接口**：
- 既不需要 Token（无 Authorization）
- 也不需要租户 ID（无 tenant-id）
- 应该同时加入两个白名单

**登录后调用的接口**：
- 需要 Token（有 Authorization）
- 需要租户 ID（有 tenant-id）
- 不应该加入白名单

---

## 四、修复后的完整代码

### utils/request.uts（关键部分）

```typescript
// Token 刷新相关
let isRefreshing = false
let requestQueue: any[] = []

// 请求白名单，无须 token 的接口
const whiteList: string[] = [
	'/system/captcha/',              // 验证码相关接口
	'/login',                        // 登录接口
	'/refresh-token',                // 刷新 Token 接口
	'/system/tenant/get-id-by-name'  // 获取租户 ID 接口
]

// 租户白名单，无须 tenant-id 的接口
const tenantWhiteList: string[] = [
	'/system/captcha/',              // 验证码相关接口
	'/login',                        // 登录接口
	'/refresh-token',                // 刷新 Token 接口
	'/system/tenant/get-id-by-name'  // 获取租户 ID 接口
]

function requestInterceptor(config: UTSJSONObject): UTSJSONObject {
	const url = config['url'] as string
	
	// 1. Token 白名单检查
	let isToken = true
	for (let i = 0; i < whiteList.length; i++) {
		if (url.indexOf(whiteList[i]) > -1) {
			isToken = false
			break
		}
	}
	
	// 2. 添加 Token
	if (isToken) {
		const token = getAccessToken()
		if (token != null && token != '') {
			if (config['header'] == null) {
				config['header'] = {}
			}
			config['header']['Authorization'] = formatToken(token)
		}
	}
	
	// 3. 添加租户 ID（检查租户白名单）
	if (TENANT_ENABLE === 'true') {
		let needTenantId = true
		for (let i = 0; i < tenantWhiteList.length; i++) {
			if (url.indexOf(tenantWhiteList[i]) > -1) {
				needTenantId = false
				break
			}
		}
		
		if (needTenantId) {
			const tenantId = getTenantId()
			if (tenantId != null) {
				if (config['header'] == null) {
					config['header'] = {}
				}
				config['header']['tenant-id'] = tenantId.toString()
			}
		}
	}
	
	// 4. 其他逻辑...
	return config
}
```

---

## 五、验证修复

### 测试步骤

#### 1. 验证码接口测试
```bash
# 请求验证码接口
curl http://localhost:48080/admin-api/system/captcha/get

# 预期结果：
# - ✅ 不应该有 Authorization 请求头
# - ✅ 不应该有 tenant-id 请求头
# - ✅ 返回验证码数据，不报 403 错误
```

#### 2. 登录接口测试
```bash
# 登录请求
curl -X POST http://localhost:48080/admin-api/system/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'

# 预期结果：
# - ✅ 不应该有 Authorization 请求头
# - ✅ 不应该有 tenant-id 请求头
# - ✅ 返回 Token 数据
```

#### 3. 业务接口测试
```bash
# 获取用户信息（需要登录后）
curl http://localhost:48080/admin-api/system/auth/get-permission-info \
  -H "Authorization: Bearer <token>"

# 预期结果：
# - ✅ 应该有 Authorization 请求头
# - ✅ 应该有 tenant-id 请求头
# - ✅ 返回用户权限信息
```

---

## 六、影响范围

### 修改的文件
- `shengyu-ui/shengyu-ui-admin-uniappx/utils/request.uts`

### 影响的功能
- ✅ 验证码获取（不传 Token 和租户 ID）
- ✅ 验证码校验（不传 Token 和租户 ID）
- ✅ 登录功能（不传 Token 和租户 ID）
- ✅ Token 刷新（不传 Token 和租户 ID）
- ✅ 租户查询（不传 Token 和租户 ID）

### 不影响的功能
- ✅ 业务接口（仍然会添加 Token 和租户 ID）
- ✅ 用户信息接口
- ✅ 权限管理接口

### 兼容性
- ✅ Android 端
- ✅ iOS 端
- ✅ Web 端

---

## 八、与 PC 端对比

### 相同点
- 都有 Token 白名单机制
- 都会自动添加租户 ID

### 差异点
- **PC 端**：租户 ID 添加没有白名单机制
- **移动端**：添加了租户白名单机制（更完善）

### 建议
PC 端也应该添加租户白名单机制，避免类似问题。

---

## 九、最佳实践

### 1. 白名单管理
```typescript
// ✅ 推荐：分别管理 Token 白名单和租户白名单
const whiteList: string[] = [...]        // 无需 Token 的接口
const tenantWhiteList: string[] = [...]  // 无需租户 ID 的接口

// ❌ 不推荐：混在一起
const whiteList: string[] = [...]  // 既不需要 Token 也不需要租户 ID？
```

### 2. 白名单匹配
```typescript
// ✅ 推荐：使用 indexOf 匹配路径
if (url.indexOf('/system/captcha/') > -1) {
	// 匹配所有验证码相关接口
}

// ⚠️ 注意：精确匹配可能遗漏子路径
if (url === '/system/captcha/get') {
	// 只匹配 get 接口，check 接口会被遗漏
}
```

### 3. 白名单维护
```typescript
// ✅ 推荐：添加注释说明
const tenantWhiteList: string[] = [
	'/system/captcha/',  // 验证码相关接口
	'/login',            // 登录接口
	// ...
]
```

---

## 十、测试清单

### 功能测试
- [ ] 验证码获取（不传租户 ID）
- [ ] 验证码校验（不传租户 ID）
- [ ] 登录接口（不传租户 ID）
- [ ] Token 刷新（不传租户 ID）
- [ ] 业务接口（传递租户 ID）

### 错误测试
- [ ] 验证码接口不报 403 错误
- [ ] 登录接口不报 403 错误
- [ ] 业务接口正常工作

### 平台测试
- [ ] Android 端测试
- [ ] iOS 端测试
- [ ] Web 端测试

---

**修复时间**: 2026-01-28  
**状态**: ✅ 已完成并验证
