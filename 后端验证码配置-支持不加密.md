# 后端验证码配置 - 支持不加密模式

## 问题说明

App 端由于 UniApp X 不支持 crypto-js，无法实现 AES 加密。需要后端支持不加密模式。

**错误信息**:
```
{
  "repCode": "0001",
  "repMsg": "Input length must be multiple of 16 when decrypting with padded cipher",
  "repData": null,
  "success": false
}
```

---

## 解决方案

### 方案一：关闭后端加密（推荐用于开发环境）

修改后端配置文件 `application.yml`:

```yaml
aj:
  captcha:
    # 验证码类型
    type: default
    # 滑动验证，底图路径
    jigsaw: classpath:images/jigsaw
    # 滑动验证，滑块路径
    pic-click: classpath:images/pic-click
    # 是否开启加密
    water-mark: false
    # 接口请求次数限制
    req-frequency-limit-enable: false
    # 验证码加密秘钥（关闭加密后不需要）
    # aes-status: false  # 关闭 AES 加密
```

或者在配置类中设置：

```java
@Configuration
public class CaptchaConfig {
    
    @Bean
    public CaptchaService captchaService() {
        Properties config = new Properties();
        config.put(Const.CAPTCHA_CACHETYPE, "local");
        config.put(Const.CAPTCHA_WATER_MARK, "false");
        config.put(Const.CAPTCHA_TYPE, "default");
        config.put(Const.CAPTCHA_AES_STATUS, "false");  // 关闭 AES 加密
        
        return CaptchaServiceFactory.getInstance(config);
    }
}
```

### 方案二：修改验证码库源码（不推荐）

如果无法修改配置，可以修改 `CaptchaService` 的实现：

```java
@Override
public ResponseModel check(CaptchaVO captchaVO) {
    String pointJson = captchaVO.getPointJson();
    
    // 判断是否需要解密
    if (StringUtils.isNotBlank(secretKey)) {
        try {
            // 尝试解密
            pointJson = AESUtil.aesDecrypt(pointJson, secretKey);
        } catch (Exception e) {
            // 解密失败，可能是明文，直接使用
            logger.warn("解密失败，使用明文: " + e.getMessage());
        }
    }
    
    // 继续验证逻辑...
}
```

### 方案三：使用原生加密插件（推荐用于生产环境）

1. **安装 UniApp X 原生加密插件**

2. **实现 AES 加密**:

```typescript
// 使用原生插件
import { AES } from '@/uni_modules/xxx-aes'

export function aesEncrypt(word: string, keyWord: string): string {
  return AES.encrypt(word, keyWord, {
    mode: 'ECB',
    padding: 'Pkcs7'
  })
}
```

---

## 当前临时方案

### App 端实现

`components/captcha/utils/aes.uts`:

```typescript
export function aesEncrypt(word: string, keyWord: string): string {
  // 临时方案：不加密，直接返回原文
  // 需要后端支持不加密模式
  return word
}
```

### 验证码组件

`components/captcha/slider-captcha.uvue`:

```typescript
async function verifyCaptcha(distance: number) {
  const pointJson = JSON.stringify({ x: distance, y: 5.0 })
  
  // 不加密，直接发送明文
  const data = {
    captchaType: props.captchaType,
    pointJson: pointJson,  // 明文
    token: token.value
  }
  
  const result = await checkCaptcha(data)
  // ...
}
```

---

## 验证配置是否生效

### 1. 查看后端日志

启动后端服务，查看日志中是否有加密相关的配置：

```
AES 加密状态: false
验证码配置加载成功
```

### 2. 测试接口

使用 Postman 测试：

```bash
POST http://localhost:48080/admin-api/system/captcha/check
Content-Type: application/json

{
  "captchaType": "blockPuzzle",
  "pointJson": "{\"x\":100,\"y\":5}",  // 明文
  "token": "xxx"
}
```

**预期响应**:
```json
{
  "repCode": "0000",
  "repMsg": "success",
  "repData": {
    "captchaVerification": "xxx"
  }
}
```

### 3. App 端测试

运行 App，滑动验证码，查看控制台日志：

```
校验验证码请求参数: {
  captchaType: "blockPuzzle",
  pointJson: "{\"x\":100,\"y\":5}",
  token: "xxx"
}

校验验证码响应: {
  repCode: "0000",
  repMsg: "success"
}
```

---

## 不同环境的配置

### 开发环境

```yaml
# application-dev.yml
aj:
  captcha:
    aes-status: false  # 关闭加密
```

### 生产环境

```yaml
# application-prod.yml
aj:
  captcha:
    aes-status: true  # 开启加密
```

然后在 App 端使用原生加密插件。

---

## 常见问题

### Q1: 关闭加密后安全性如何？

A: 
- **开发环境**: 可以关闭加密，方便调试
- **生产环境**: 必须开启加密，使用 HTTPS + AES 双重保护
- **建议**: 使用原生加密插件实现真正的 AES 加密

### Q2: 如何判断后端是否支持不加密？

A: 发送明文数据，如果返回 `repCode: "0000"` 说明支持，如果返回加密错误说明不支持。

### Q3: 原生加密插件如何选择？

A: 
- 搜索 UniApp 插件市场
- 选择支持 UniApp X 的 AES 加密插件
- 确保支持 ECB 模式和 Pkcs7 填充

---

## 推荐配置（开发环境）

### 后端配置

```yaml
# application-dev.yml
aj:
  captcha:
    type: default
    jigsaw: classpath:images/jigsaw
    pic-click: classpath:images/pic-click
    water-mark: false
    req-frequency-limit-enable: false
    aes-status: false  # 关闭加密
```

### App 端配置

```typescript
// config/app.config.uts
export const BASE_URL = 'http://localhost:48080'
export const CAPTCHA_ENABLE = 'true'
```

### 验证码组件

```typescript
// 不加密，直接发送明文
const pointJson = JSON.stringify({ x: distance, y: 5.0 })
const data = {
  captchaType: props.captchaType,
  pointJson: pointJson,  // 明文
  token: token.value
}
```

---

## 总结

1. **开发环境**: 关闭后端加密，App 端发送明文
2. **生产环境**: 使用原生加密插件，实现真正的 AES 加密
3. **安全性**: 生产环境必须使用 HTTPS + AES 加密

---

**更新时间**: 2026-01-27  
**版本**: v1.2.0
