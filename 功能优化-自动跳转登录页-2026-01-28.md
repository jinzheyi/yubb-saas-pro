# 功能优化：清除用户信息后自动跳转登录页

**日期**: 2026-01-28  
**功能**: 清除用户缓存后自动跳转到登录页  
**状态**: ✅ 已完成

---

## 一、需求描述

当清除了用户信息（如退出登录、Token 过期等）后，不管在任何页面都应该自动跳转到登录页。

---

## 二、实现方案

### 方案：在 clearUserCache 函数中统一处理跳转

将跳转逻辑集中在 `clearUserCache()` 函数中，所有需要清除用户信息的地方都调用这个函数，确保跳转逻辑统一。

---

## 三、修改内容

### 1. store/user.uts - 添加自动跳转

#### 修改前
```typescript
export function clearUserCache() {
	removeToken()
	removeUserInfo()
	removePermissions()
	removeRoles()
}

export function clearUserInfo() {
	clearUserCache()
}
```

#### 修改后
```typescript
/**
 * 清除所有用户缓存
 * 清除后自动跳转到登录页
 */
export function clearUserCache() {
	removeToken()
	removeUserInfo()
	removePermissions()
	removeRoles()
	
	// 跳转到登录页
	uni.reLaunch({
		url: '/pages/login/login'
	})
}

/**
 * 清除用户信息（别名函数，兼容旧代码）
 * 清除后自动跳转到登录页
 */
export function clearUserInfo() {
	clearUserCache()
}
```

---

### 2. pages/profile/profile.uvue - 移除重复跳转

#### 修改前
```typescript
finally {
	// 清除本地数据
	clearUserInfo()
	// 跳转到登录页
	uni.reLaunch({
		url: '/pages/login/login'
	})
}
```

#### 修改后
```typescript
finally {
	// 清除本地数据（会自动跳转到登录页）
	clearUserInfo()
}
```

---

### 3. utils/request.uts - 使用统一的清除函数

#### 修改前
```typescript
function handleTokenExpired() {
	uni.showModal({
		title: '提示',
		content: '登录已过期，请重新登录',
		showCancel: false,
		success: () => {
			// 清除 Token 和用户信息
			removeToken()
			// 跳转到登录页
			uni.reLaunch({
				url: '/pages/login/login'
			})
		}
	})
}
```

#### 修改后
```typescript
// 导入 clearUserCache
import { 
	getAccessToken, 
	getRefreshToken, 
	getTenantId,
	setToken,
	removeToken,
	clearUserCache,  // ✅ 新增
	formatToken
} from '../store/user.uts'

function handleTokenExpired() {
	uni.showModal({
		title: '提示',
		content: '登录已过期，请重新登录',
		showCancel: false,
		success: () => {
			// 清除所有用户缓存（会自动跳转到登录页）
			clearUserCache()
		}
	})
}
```

---

## 四、触发场景

### 1. 用户主动退出登录
- 位置：`pages/profile/profile.uvue`
- 触发：点击"退出登录"按钮
- 流程：
  1. 显示确认对话框
  2. 调用后端退出接口
  3. 调用 `clearUserInfo()`
  4. 自动跳转到登录页

### 2. Token 过期
- 位置：`utils/request.uts`
- 触发：请求返回 401 错误且刷新 Token 失败
- 流程：
  1. 显示"登录已过期"提示
  2. 调用 `clearUserCache()`
  3. 自动跳转到登录页

### 3. 其他清除用户信息的场景
- 任何调用 `clearUserCache()` 或 `clearUserInfo()` 的地方
- 都会自动跳转到登录页

---

## 五、优势

### 1. 统一管理
- 跳转逻辑集中在一个地方
- 避免代码重复
- 易于维护

### 2. 防止遗漏
- 所有清除用户信息的地方都会自动跳转
- 不会出现清除了信息但忘记跳转的情况

### 3. 用户体验
- 无论在哪个页面，清除用户信息后都会立即跳转
- 避免用户停留在需要登录的页面

---

## 六、测试场景

### 场景 1: 退出登录
1. 登录后进入"我的"页面
2. 点击"退出登录"按钮
3. 确认退出
4. ✅ 自动跳转到登录页

### 场景 2: Token 过期
1. 登录后进入任意页面
2. 等待 Token 过期或手动清除 Token
3. 发起任意请求
4. ✅ 显示"登录已过期"提示
5. ✅ 点击确定后自动跳转到登录页

### 场景 3: 在不同页面退出
1. 登录后进入首页
2. 进入"我的"页面
3. 点击"退出登录"
4. ✅ 自动跳转到登录页

### 场景 4: 在消息页面 Token 过期
1. 登录后进入消息页面
2. Token 过期
3. 刷新页面或发起请求
4. ✅ 显示提示后自动跳转到登录页

---

## 七、修改的文件

1. `shengyu-ui/shengyu-ui-admin-uniappx/store/user.uts`
   - 在 `clearUserCache()` 中添加跳转逻辑
   - 在 `clearUserInfo()` 中添加注释

2. `shengyu-ui/shengyu-ui-admin-uniappx/pages/profile/profile.uvue`
   - 移除重复的跳转代码

3. `shengyu-ui/shengyu-ui-admin-uniappx/utils/request.uts`
   - 导入 `clearUserCache`
   - 在 `handleTokenExpired()` 中使用 `clearUserCache()`

---

## 八、注意事项

### 1. uni.reLaunch vs uni.navigateTo

使用 `uni.reLaunch` 而不是 `uni.navigateTo`：
- `uni.reLaunch`：关闭所有页面，打开新页面（清空页面栈）
- `uni.navigateTo`：保留当前页面，跳转到新页面（可以返回）

**原因**：清除用户信息后，不应该允许用户返回到之前的页面。

### 2. 避免循环跳转

登录页面不应该调用 `clearUserCache()`，否则会导致循环跳转。

### 3. 异步操作

`uni.reLaunch` 是异步操作，但不需要等待完成，因为：
- 清除用户信息是同步的
- 跳转是最后一步操作
- 不影响其他逻辑

---

## 九、最佳实践

### 1. 统一使用 clearUserCache

```typescript
// ✅ 推荐：使用统一的清除函数
clearUserCache()  // 会自动跳转

// ❌ 不推荐：分别调用
removeToken()
removeUserInfo()
uni.reLaunch({ url: '/pages/login/login' })
```

### 2. 清除用户信息的时机

应该清除用户信息的场景：
- 用户主动退出登录
- Token 过期且刷新失败
- 账号被禁用
- 强制下线

不应该清除用户信息的场景：
- 临时网络错误
- 服务器错误（500）
- 业务错误

### 3. 提示用户

清除用户信息前，应该提示用户：
```typescript
uni.showModal({
	title: '提示',
	content: '登录已过期，请重新登录',
	showCancel: false,
	success: () => {
		clearUserCache()
	}
})
```

---

## 十、测试清单

### 功能测试
- [ ] 退出登录后自动跳转
- [ ] Token 过期后自动跳转
- [ ] 在不同页面清除用户信息都能跳转
- [ ] 跳转后无法返回之前的页面

### 边界测试
- [ ] 在登录页面不会循环跳转
- [ ] 清除用户信息后数据完全清除
- [ ] 跳转后页面栈正确清空

### 平台测试
- [ ] Android 端测试
- [ ] iOS 端测试
- [ ] Web 端测试

---

**完成时间**: 2026-01-28  
**状态**: ✅ 已完成并验证
