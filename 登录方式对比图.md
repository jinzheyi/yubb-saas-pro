# 登录方式对比图

## 账号密码登录

```
┌─────────────────────────────────────┐
│         圣钰管理系统                 │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 租户名称                       │ │
│  │ [圣钰科技___________________] │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 账号                           │ │
│  │ [admin_____________________] │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 密码                           │ │
│  │ [••••••••__________________] │ │
│  └───────────────────────────────┘ │
│                                     │
│  ☑ 记住我                           │
│                                     │
│  ┌───────────────────────────────┐ │
│  │         登  录                 │ │
│  └───────────────────────────────┘ │
│                                     │
│         手机号登录                   │
│                                     │
└─────────────────────────────────────┘

特点：
✅ 显示租户名称输入框
✅ 显示记住我选项
✅ 需要滑块验证码
✅ 需要获取租户ID
```

## 手机号登录

```
┌─────────────────────────────────────┐
│         圣钰管理系统                 │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 手机号                         │ │
│  │ [13800138000_______________] │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌─────────────────┬─────────────┐ │
│  │ 验证码           │ 获取验证码   │ │
│  │ [123456_______] │   (60s)    │ │
│  └─────────────────┴─────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │         登  录                 │ │
│  └───────────────────────────────┘ │
│                                     │
│         账号密码登录                 │
│                                     │
└─────────────────────────────────────┘

特点：
❌ 不显示租户名称输入框 ⭐
❌ 不显示记住我选项
❌ 不需要滑块验证码
❌ 不需要获取租户ID
✅ 使用短信验证码
```

## 登录流程对比

### 账号密码登录流程

```
用户输入
  ↓
┌─────────────────┐
│ 租户名称         │
│ 账号             │
│ 密码             │
└─────────────────┘
  ↓
点击登录
  ↓
┌─────────────────┐
│ 表单验证         │
└─────────────────┘
  ↓
┌─────────────────┐
│ 显示滑块验证码   │
└─────────────────┘
  ↓
验证成功
  ↓
┌─────────────────┐
│ 获取租户ID       │
│ (通过租户名称)   │
└─────────────────┘
  ↓
┌─────────────────┐
│ 调用登录接口     │
└─────────────────┘
  ↓
┌─────────────────┐
│ 保存Token        │
│ 加载用户信息     │
└─────────────────┘
  ↓
┌─────────────────┐
│ 处理"记住我"     │
└─────────────────┘
  ↓
跳转首页
```

### 手机号登录流程

```
用户输入
  ↓
┌─────────────────┐
│ 手机号           │
└─────────────────┘
  ↓
点击获取验证码
  ↓
┌─────────────────┐
│ 手机号格式验证   │
└─────────────────┘
  ↓
┌─────────────────┐
│ 发送短信验证码   │
│ (60秒倒计时)     │
└─────────────────┘
  ↓
用户输入验证码
  ↓
点击登录
  ↓
┌─────────────────┐
│ 表单验证         │
└─────────────────┘
  ↓
┌─────────────────┐
│ 调用登录接口     │
│ (无需租户ID)     │ ⭐
└─────────────────┘
  ↓
┌─────────────────┐
│ 保存Token        │
│ 加载用户信息     │
└─────────────────┘
  ↓
跳转首页
```

## 关键差异总结

| 项目 | 账号登录 | 手机号登录 |
|------|---------|-----------|
| 租户名称 | ✅ 需要输入 | ❌ 不需要 ⭐ |
| 滑块验证码 | ✅ 需要 | ❌ 不需要 |
| 短信验证码 | ❌ 不需要 | ✅ 需要 |
| 记住我 | ✅ 支持 | ❌ 不支持 |
| 获取租户ID | ✅ 需要 | ❌ 不需要 ⭐ |

## 核心优化点

### ⭐ 手机号登录不显示租户名称

**原因**：
1. 移动端界面更简洁
2. 后端根据手机号自动识别租户
3. 提升用户体验，减少输入步骤

**实现**：
```vue
<!-- 条件渲染 -->
<view v-if="loginType === 'account'">
  <!-- 显示租户名称 -->
  <input v-model="tenantName" />
</view>

<view v-else>
  <!-- 不显示租户名称 -->
  <input v-model="mobile" />
</view>
```

**逻辑处理**：
```typescript
async function doLogin(verification: string) {
  // 仅账号登录需要获取租户ID
  if (loginType.value === 'account') {
    await fetchTenantId()
  }
  
  // 手机号登录直接调用接口
  if (loginType.value === 'account') {
    tokenData = await login({ username, password, captchaVerification })
  } else {
    tokenData = await smsLogin({ mobile, code }) // 无需租户ID
  }
}
```

## 用户体验对比

### 账号登录
```
步骤数：6 步
1. 输入租户名称
2. 输入账号
3. 输入密码
4. 勾选记住我（可选）
5. 点击登录
6. 完成滑块验证码

优点：
- 支持多租户
- 安全性高（双重验证）
- 支持记住我

适用场景：
- 企业内部员工
- 管理员
- 需要多租户隔离
```

### 手机号登录
```
步骤数：4 步
1. 输入手机号
2. 获取验证码
3. 输入验证码
4. 点击登录

优点：
- 步骤更少 ⭐
- 无需记住租户名称 ⭐
- 操作更简单

适用场景：
- 移动端用户
- 临时访问
- 快速登录
```

## 技术实现对比

### PC 端（Vue3）

**文件结构**：
```
src/views/Login/components/
├── LoginForm.vue      # 账号登录（显示租户名称）
├── MobileForm.vue     # 手机号登录（不显示租户名称）
└── LoginFormTitle.vue # 标题组件
```

**特点**：
- 两个独立组件
- 通过路由切换
- Element Plus UI

### 移动端（UniApp X）

**文件结构**：
```
pages/login/
└── login.uvue         # 统一登录页（条件渲染）
```

**特点**：
- 单个组件
- 通过 v-if 切换
- 原生组件

## 总结

✅ **核心目标达成**：手机号登录不显示租户名称

✅ **逻辑统一**：PC 端和移动端完全一致

✅ **用户体验**：移动端更简洁，操作更便捷

✅ **代码质量**：结构清晰，易于维护
