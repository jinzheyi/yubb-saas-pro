# 验证码组件 - 下一步实现指南

## 快速开始

### 当前状态
✅ 主验证码组件已创建 (`verify.uvue`)
✅ AES工具框架已创建 (`aes.uts`)
⏳ 滑块组件需要重构 (`verify-slider.uvue`)
⏳ 点选组件需要创建 (`verify-point.uvue`)

## 实现步骤

### 步骤1: 重构滑块验证码组件

**文件**: `shengyu-ui/shengyu-ui-admin-uniappx/components/captcha/verify-slider.uvue`

**参考文件**:
- 现有实现: `slider-captcha.uvue`
- 示例代码: `shengyu-ui/captcha-dev/view/uni-app/src/pages/verify/verifySlider/verifySlider.vue`

**核心代码模板**:
```vue
<template>
  <view>
    <!-- 背景图 -->
    <image :src="'data:image/png;base64,' + backImgBase" />
    
    <!-- 滑块图 -->
    <view class="verify-sub-block" :style="{ left: moveBlockLeft }">
      <image :src="'data:image/png;base64,' + blockBackImgBase" />
    </view>
    
    <!-- 滑动条 -->
    <view class="verify-bar-area">
      <view class="verify-move-block"
        @touchstart="handleTouchStart"
        @touchmove="handleTouchMove"
        @touchend="handleTouchEnd"
      />
    </view>
  </view>
</template>

<script setup lang="uts">
// 1. 获取验证码
async function getPictrue() {
  const data = {
    captchaType: props.captchaType,
    clientUid: getUUID('slider'),
    ts: Date.now()
  }
  const res = await request({ url: '/system/captcha/get', data })
  // 保存图片和token
}

// 2. 触摸事件
function handleTouchStart(e) {
  startTime = Date.now()
  startX = e.touches[0].pageX
}

function handleTouchMove(e) {
  const moveX = e.touches[0].pageX - startX
  // 更新滑块位置
}

function handleTouchEnd(e) {
  // 计算距离并转换坐标
  const distance = parseInt(moveBlockLeft) * 310 / parseInt(imgSize.width)
  
  // 提交验证
  const pointJson = JSON.stringify({ x: distance, y: 5.0 })
  const encrypted = aesEncrypt(pointJson, secretKey)
  
  await request({
    url: '/system/captcha/check',
    data: { captchaType, pointJson: encrypted, token }
  })
}
</script>
```

### 步骤2: 创建点选验证码组件

**文件**: `shengyu-ui/shengyu-ui-admin-uniappx/components/captcha/verify-point.uvue`

**参考文件**:
- 示例代码: `shengyu-ui/captcha-dev/view/uni-app/src/pages/verify/verifyPoint/verifyPoint.vue`

**核心代码模板**:
```vue
<template>
  <view>
    <!-- 背景图 -->
    <image 
      :src="'data:image/png;base64,' + pointBackImgBase"
      @click="handleClick"
    />
    
    <!-- 点击标记 -->
    <view v-for="(point, index) in tempPoints" :key="index"
      class="point-area"
      :style="{ left: point.x + 'px', top: point.y + 'px' }"
    >
      {{ index + 1 }}
    </view>
    
    <!-- 提示文字 -->
    <view class="verify-bar-area">
      <text>{{ text }}</text>
    </view>
  </view>
</template>

<script setup lang="uts">
// 1. 获取验证码
async function getPictrue() {
  const data = {
    captchaType: props.captchaType,
    clientUid: getUUID('point'),
    ts: Date.now()
  }
  const res = await request({ url: '/system/captcha/get', data })
  // 保存图片、token和文字列表
  text.value = '请依次点击【' + wordList.join(',') + '】'
}

// 2. 点击事件
function handleClick(e) {
  // 获取图片位置
  uni.createSelectorQuery().in(this)
    .select('#image')
    .boundingClientRect(data => {
      const x = e.detail.x - data.left
      const y = e.detail.y - data.top
      
      checkPosArr.push({ x, y })
      tempPoints.push({ x, y })
      
      if (checkPosArr.length === checkNum) {
        submitVerify()
      }
    }).exec()
}

// 3. 提交验证
async function submitVerify() {
  // 坐标转换
  const transformed = checkPosArr.map(p => ({
    x: Math.round(310 * p.x / parseInt(imgSize.width)),
    y: Math.round(155 * p.y / parseInt(imgSize.height))
  }))
  
  // 加密并提交
  const pointJson = JSON.stringify(transformed)
  const encrypted = aesEncrypt(pointJson, secretKey)
  
  await request({
    url: '/system/captcha/check',
    data: { captchaType, pointJson: encrypted, token }
  })
}
</script>
```

### 步骤3: 实现AES加密

**文件**: `shengyu-ui/shengyu-ui-admin-uniappx/components/captcha/utils/aes.uts`

**需要做的**:
1. 引入加密库（crypto-js或原生API）
2. 实现ECB模式的AES加密
3. 测试加密结果

**临时方案**（如果无法引入库）:
```typescript
// 使用原生API或简化实现
// 注意：生产环境必须使用真正的AES加密
export function aesEncrypt(word: string, keyStr: string): string {
  // TODO: 实现真正的AES加密
  // 临时返回原文（仅用于开发测试）
  console.warn('AES加密未实现')
  return word
}
```

### 步骤4: 更新登录页面

**文件**: `shengyu-ui/shengyu-ui-admin-uniappx/pages/login/login.uvue`

**修改内容**:
```vue
<template>
  <view v-if="loginType === 'account'">
    <!-- 替换旧的验证码组件 -->
    <verify
      v-if="captchaEnable === 'true'"
      ref="verifyRef"
      :captchaType="'blockPuzzle'"
      :mode="'fixed'"
      @success="handleCaptchaSuccess"
    />
  </view>
</template>

<script setup lang="uts">
import verify from '@/components/captcha/verify.uvue'

const verifyRef = ref<any>(null)
const captchaVerification = ref<string>('')

function handleCaptchaSuccess(e: any) {
  captchaVerification.value = e.captchaVerification
  console.log('验证成功:', captchaVerification.value)
}

async function handleLogin() {
  if (captchaEnable.value === 'true' && !captchaVerification.value) {
    uni.showToast({ title: '请完成验证码验证', icon: 'none' })
    return
  }
  
  // 提交登录请求，包含 captchaVerification
  const data = {
    username: username.value,
    password: password.value,
    captchaVerification: captchaVerification.value
  }
  
  // ... 登录逻辑
}
</script>
```

## 测试清单

### 功能测试
```
[ ] 滑块验证码
  [ ] 图片正常显示
  [ ] 可以拖动滑块
  [ ] 验证成功提示
  [ ] 验证失败提示
  [ ] 刷新功能

[ ] 点选验证码
  [ ] 图片正常显示
  [ ] 可以点击选择
  [ ] 点击标记显示
  [ ] 验证成功提示
  [ ] 验证失败提示
  [ ] 刷新功能

[ ] 模式切换
  [ ] 弹窗模式正常
  [ ] 固定模式正常
  [ ] 模式切换无问题

[ ] 登录集成
  [ ] 账号登录显示验证码
  [ ] 手机号登录不显示验证码
  [ ] 验证成功后可以登录
  [ ] 验证失败后不能登录
```

### 兼容性测试
```
[ ] Android设备
  [ ] 触摸事件正常
  [ ] 图片显示正常
  [ ] 坐标计算准确

[ ] iOS设备
  [ ] 触摸事件正常
  [ ] 图片显示正常
  [ ] 坐标计算准确

[ ] 不同屏幕尺寸
  [ ] 小屏幕（<375px）
  [ ] 中屏幕（375-414px）
  [ ] 大屏幕（>414px）
```

## 常见问题

### Q1: 坐标不准确怎么办？
**A**: 检查坐标转换公式，确保使用标准尺寸310x155进行转换。

### Q2: 图片不显示怎么办？
**A**: 检查base64格式是否正确，确保添加了 `data:image/png;base64,` 前缀。

### Q3: AES加密失败怎么办？
**A**: 临时可以不加密直接传输（仅开发环境），生产环境必须实现真正的加密。

### Q4: 触摸事件不响应怎么办？
**A**: 检查元素的 `touch-action` 样式，确保没有被禁用。

## 参考资料

- **实现方案**: `App端验证码完整实现方案.md`
- **实现说明**: `App端验证码组件-实现说明.md`
- **示例代码**: `shengyu-ui/captcha-dev/view/uni-app`
- **服务端接口**: `CaptchaController.java`
- **官方文档**: https://ajcaptcha.beliefteam.cn/captcha-doc/captchaDoc/uni-app.html

## 预计时间

- 滑块组件重构: 2-3小时
- 点选组件创建: 2-3小时
- AES加密实现: 1-2小时
- 登录页面集成: 1小时
- 测试和调试: 2-3小时

**总计**: 8-12小时

## 完成标准

✅ 滑块验证码可以正常使用
✅ 点选验证码可以正常使用
✅ 验证成功后可以登录
✅ 在Android和iOS设备上测试通过
✅ 代码符合项目规范
✅ 文档完善

---

**开始实现吧！有任何问题随时查看参考文档。**
