package com.shengyu.framework.netty.core;import com.shengyu.framework.netty.config.NettyProperties;import io.netty.bootstrap.ServerBootstrap;import io.netty.channel.ChannelOption;import io.netty.channel.EventLoopGroup;import io.netty.channel.nio.NioEventLoopGroup;import io.netty.channel.socket.nio.NioServerSocketChannel;import lombok.extern.slf4j.Slf4j;import org.springframework.beans.factory.InitializingBean;/** * netty服务 * * @author zhusy * @since 2022/11/13 */@Slf4jpublic class NettyServer implements InitializingBean {    private final NettyProperties nettyProperties;    public NettyServer(NettyProperties nettyProperties) {        this.nettyProperties = nettyProperties;    }    @Override    public void afterPropertiesSet() throws Exception {        if (!nettyProperties.isEnable()) {            return;        }        //boss线程用于处理连接工作        EventLoopGroup bossGroup = new NioEventLoopGroup(nettyProperties.getBossThread());        //work线程用于数据处理        EventLoopGroup workGroup = new NioEventLoopGroup(nettyProperties.getWorkerThread());        ServerBootstrap serverBootstrap = new ServerBootstrap()                .group(bossGroup, workGroup)                //临时存放已经完成三次握手的请求的队列的最大长度，对应TCP/ip协议listen函数中backlog参数。                .option(ChannelOption.SO_BACKLOG, nettyProperties.getBackLog())                //设置TCP长连接，一般如果两个小时内没有数据的通信时，TCP会自动发送一个活动探测数据报文                .childOption(ChannelOption.SO_KEEPALIVE, Boolean.TRUE)                //将小的数据包包装成更大的帧进行传送，提高网络的负载                .childOption(ChannelOption.TCP_NODELAY, Boolean.TRUE)                //设置nio模型，NioServerSocketChannel是对nio类型的链接的抽象                .channel(NioServerSocketChannel.class)                //工作线程处理业务工作类                .childHandler(new NettyWorkGroupPipelines(nettyProperties));        bind(serverBootstrap, nettyProperties.getPort());    }    private void bind(final ServerBootstrap serverBootstrap, final int port) {        serverBootstrap.bind(port).addListener(future -> {            if (future.isSuccess()) {                log.info("netty端口[{}]绑定成功!", port);            } else {                log.info("端口[{}]绑定失败!", port);            }        });    }}