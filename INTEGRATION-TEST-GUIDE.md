# IM 系统前后端联调测试指南

> **文档版本**: v1.0  
> **创建日期**: 2026年2月16日  
> **目标**: 指导开发者完成 IM 系统的前后端联调测试  
> **前置条件**: 后端服务已启动，移动端已完成开发

---

## 📋 测试前准备

### 1. 环境检查

**后端环境**:
- ✅ MySQL 数据库已启动（端口 3306）
- ✅ Redis 已启动（端口 6379）
- ✅ IM 表结构已执行（6张表）
- ✅ 后端服务已启动（端口 48080）
- ✅ WebSocket 服务已启动（端口 9000）

**移动端环境**:
- ✅ uni-app x 开发环境已配置
- ✅ 移动端代码已编译
- ✅ 测试设备已连接（Android/iOS/H5）

### 2. 配置检查

**后端配置** (`application.yaml`):
```yaml
# WebSocket 配置
shengyu:
  websocket:
    enabled: true
    port: 9000
    boss-thread-count: 1
    worker-thread-count: 4
```

**移动端配置** (`config/app.config.uts`):
```typescript
// API 基础地址
export const BASE_URL = 'http://localhost:48080'

// WebSocket 地址
export const WS_URL = 'ws://localhost:9000/ws'
```

### 3. 测试账号准备

创建至少 3 个测试账号用于测试：
- 账号1：测试用户A（用于单聊发送方）
- 账号2：测试用户B（用于单聊接收方）
- 账号3：测试用户C（用于群聊测试）

---

## 🧪 测试用例清单

### 阶段 1: 基础功能测试（P0）

#### 1.1 用户登录测试

**测试目标**: 验证用户登录和 Token 获取

**测试步骤**:
1. 打开移动端应用
2. 输入测试账号和密码
3. 点击登录按钮
4. 验证登录成功并跳转到首页

**预期结果**:
- ✅ 登录成功，获取 accessToken 和 refreshToken
- ✅ Token 保存到本地存储
- ✅ 用户信息保存到 Store
- ✅ 跳转到消息列表页面

**验证方法**:
```typescript
// 检查 Token
console.log('Access Token:', getToken())
console.log('Refresh Token:', getRefreshToken())

// 检查用户信息
console.log('User Info:', getUserInfo())
```

---

#### 1.2 WebSocket 连接测试

**测试目标**: 验证 WebSocket 连接建立和认证

**测试步骤**:
1. 登录成功后，自动建立 WebSocket 连接
2. 发送认证请求（携带 accessToken）
3. 接收认证响应
4. 验证连接状态

**预期结果**:
- ✅ WebSocket 连接成功（状态：OPEN）
- ✅ 认证请求发送成功
- ✅ 认证响应返回成功（success: true）
- ✅ 心跳开始（每 30 秒一次）

**验证方法**:
```typescript
// 检查连接状态
import { wsManager } from '@/utils/websocket.uts'
console.log('WebSocket State:', wsManager.getState())

// 监听连接状态
wsManager.addStateListener((state) => {
  console.log('WebSocket State Changed:', state)
})
```

**常见问题**:
- 连接失败：检查 WebSocket 服务是否启动
- 认证失败：检查 Token 是否有效
- 心跳超时：检查网络连接

---

#### 1.3 联系人列表测试

**测试目标**: 验证联系人列表加载

**测试步骤**:
1. 进入通讯录页面
2. 查看联系人列表
3. 验证联系人按拼音分组
4. 测试搜索功能

**预期结果**:
- ✅ 联系人列表加载成功
- ✅ 按拼音首字母分组显示
- ✅ 显示部门信息
- ✅ 搜索功能正常

**API 接口**:
- `GET /app-api/system/im/contact/list`

---

#### 1.4 群组列表测试

**测试目标**: 验证群组列表加载

**测试步骤**:
1. 进入"我的群组"页面
2. 查看群组列表
3. 点击群组查看详情

**预期结果**:
- ✅ 群组列表加载成功
- ✅ 显示群组名称、头像、成员数
- ✅ 点击可进入群聊

**API 接口**:
- `GET /app-api/system/im/group/list`

---

### 阶段 2: 消息收发测试（P0）

#### 2.1 单聊文本消息测试

**测试目标**: 验证单聊文本消息收发

**测试步骤**:
1. 用户A 进入与用户B 的聊天页面
2. 用户A 发送文本消息："你好"
3. 用户B 接收消息
4. 用户B 回复消息："你好，收到"
5. 用户A 接收回复

**预期结果**:
- ✅ 消息发送成功（状态：success）
- ✅ 消息实时送达（通过 WebSocket）
- ✅ 消息显示在聊天列表
- ✅ 会话列表更新（最后消息、时间）
- ✅ 未读数增加

**验证方法**:
```typescript
// 发送消息
const message = messageService.sendTextMessage(
  receiverId,  // 接收者ID
  0,           // 群组ID（单聊为0）
  '你好',      // 消息内容
  []           // @用户列表
)

// 监听消息
messageService.addMessageListener((message) => {
  console.log('收到新消息:', message)
})
```

---

#### 2.2 群聊文本消息测试

**测试目标**: 验证群聊文本消息收发

**测试步骤**:
1. 用户A、B、C 都加入同一个群组
2. 用户A 在群里发送消息："大家好"
3. 用户B 和 C 接收消息
4. 用户B 回复消息
5. 用户A 和 C 接收回复

**预期结果**:
- ✅ 消息发送成功
- ✅ 所有群成员都能收到消息
- ✅ 消息显示发送者昵称
- ✅ 会话列表更新

---

#### 2.3 图片消息测试

**测试目标**: 验证图片消息上传和发送

**测试步骤**:
1. 点击"+"按钮，选择"相册"
2. 选择一张图片
3. 等待上传完成
4. 发送图片消息
5. 接收方查看图片

**预期结果**:
- ✅ 图片上传成功（显示上传进度）
- ✅ 图片消息发送成功
- ✅ 接收方能看到图片
- ✅ 点击图片可预览

**API 接口**:
- `POST /platform-api/infra/file/upload` (上传图片)

---

#### 2.4 语音消息测试

**测试目标**: 验证语音消息录制和发送

**测试步骤**:
1. 切换到语音模式
2. 按住"按住 说话"按钮
3. 录制语音（5秒）
4. 松开按钮
5. 等待上传完成
6. 接收方收到语音消息

**预期结果**:
- ✅ 录音功能正常
- ✅ 显示录音时长
- ✅ 上滑可取消录音
- ✅ 语音上传成功
- ✅ 接收方能播放语音

**权限检查**:
- Android: `RECORD_AUDIO`
- iOS: `NSMicrophoneUsageDescription`

---

#### 2.5 视频消息测试

**测试目标**: 验证视频消息上传和发送

**测试步骤**:
1. 点击"+"按钮，选择"视频"
2. 选择一个视频文件
3. 等待上传完成
4. 发送视频消息
5. 接收方查看视频

**预期结果**:
- ✅ 视频上传成功
- ✅ 显示视频封面
- ✅ 接收方能播放视频

---

#### 2.6 文件消息测试

**测试目标**: 验证文件消息上传和发送

**测试步骤**:
1. 点击"+"按钮，选择"文件"
2. 选择一个文件（PDF/Word/Excel）
3. 等待上传完成
4. 发送文件消息
5. 接收方查看文件信息

**预期结果**:
- ✅ 文件上传成功
- ✅ 显示文件名和大小
- ✅ 接收方能看到文件信息
- ✅ 支持 H5、Android、iOS 三端

---

### 阶段 3: 会话管理测试（P0）

#### 3.1 会话列表测试

**测试目标**: 验证会话列表加载和更新

**测试步骤**:
1. 进入消息列表页面
2. 查看会话列表
3. 发送新消息
4. 验证会话列表更新

**预期结果**:
- ✅ 会话列表加载成功
- ✅ 显示最后消息内容
- ✅ 显示未读数
- ✅ 新消息到达时会话置顶
- ✅ 时间显示正确

**API 接口**:
- `GET /app-api/system/im/conversation/list`

---

#### 3.2 会话置顶测试

**测试目标**: 验证会话置顶功能

**测试步骤**:
1. 长按会话
2. 点击"置顶"
3. 验证会话移到顶部
4. 再次点击"取消置顶"

**预期结果**:
- ✅ 置顶成功，会话移到顶部
- ✅ 置顶标识显示
- ✅ 取消置顶后恢复原位置

**API 接口**:
- `PUT /app-api/system/im/conversation/pin`

---

#### 3.3 清空未读数测试

**测试目标**: 验证清空未读数功能

**测试步骤**:
1. 进入有未读消息的会话
2. 验证未读数清空
3. 返回会话列表
4. 验证未读数为 0

**预期结果**:
- ✅ 进入会话时自动清空未读数
- ✅ 会话列表未读数更新为 0

**API 接口**:
- `PUT /app-api/system/im/conversation/clear-unread-count`

---

#### 3.4 删除会话测试

**测试目标**: 验证删除会话功能

**测试步骤**:
1. 长按会话
2. 点击"删除"
3. 确认删除
4. 验证会话从列表中移除

**预期结果**:
- ✅ 会话删除成功
- ✅ 会话从列表中移除
- ✅ 聊天记录保留（仅删除会话）

**API 接口**:
- `DELETE /app-api/system/im/conversation/delete`

---

### 阶段 4: 消息操作测试（P1）

#### 4.1 消息撤回测试

**测试目标**: 验证消息撤回功能

**测试步骤**:
1. 发送一条消息
2. 长按消息
3. 点击"撤回"
4. 确认撤回
5. 验证消息显示为"你撤回了一条消息"

**预期结果**:
- ✅ 撤回成功
- ✅ 消息显示为撤回状态
- ✅ 接收方也看到撤回提示

**API 接口**:
- `POST /app-api/system/im/message/recall`

---

#### 4.2 消息删除测试

**测试目标**: 验证消息删除功能

**测试步骤**:
1. 长按消息
2. 点击"删除"
3. 确认删除
4. 验证消息从列表中移除

**预期结果**:
- ✅ 消息删除成功
- ✅ 消息从列表中移除
- ✅ 仅本地删除，不影响对方

**API 接口**:
- `DELETE /app-api/system/im/message/delete`

---

### 阶段 5: 群组管理测试（P1）

#### 5.1 创建群组测试

**测试目标**: 验证创建群组功能

**测试步骤**:
1. 点击"发起群聊"
2. 选择群成员（至少2人）
3. 输入群名称
4. 点击"完成"
5. 验证群组创建成功

**预期结果**:
- ✅ 群组创建成功
- ✅ 群成员收到入群通知
- ✅ 群聊会话自动创建

**API 接口**:
- `POST /app-api/system/im/group/create`

---

#### 5.2 群成员管理测试

**测试目标**: 验证群成员添加和移除

**测试步骤**:
1. 进入群聊设置
2. 点击"添加成员"
3. 选择新成员
4. 验证成员添加成功
5. 点击"移除成员"
6. 验证成员移除成功

**预期结果**:
- ✅ 成员添加成功
- ✅ 成员移除成功
- ✅ 群成员列表更新

**API 接口**:
- `POST /app-api/system/im/group/member/add`
- `DELETE /app-api/system/im/group/member/remove`

---

## 🐛 常见问题排查

### 1. WebSocket 连接失败

**症状**: 无法建立 WebSocket 连接

**排查步骤**:
1. 检查 WebSocket 服务是否启动
2. 检查端口是否正确（9000）
3. 检查防火墙设置
4. 检查网络连接

**解决方案**:
```bash
# 检查 WebSocket 服务
netstat -an | grep 9000

# 查看后端日志
tail -f shengyu-server/logs/info.log
```

---

### 2. 消息发送失败

**症状**: 消息发送后状态为 fail

**排查步骤**:
1. 检查 WebSocket 连接状态
2. 检查消息格式是否正确
3. 检查后端日志

**解决方案**:
```typescript
// 检查连接状态
console.log('WebSocket State:', wsManager.getState())

// 检查消息内容
console.log('Message:', message)
```

---

### 3. 文件上传失败

**症状**: 文件上传返回错误

**排查步骤**:
1. 检查文件大小是否超限
2. 检查文件类型是否支持
3. 检查上传接口是否正常
4. 检查 Token 是否有效

**解决方案**:
```bash
# 测试上传接口
curl -X POST http://localhost:48080/platform-api/infra/file/upload \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@test.jpg"
```

---

### 4. 权限请求失败

**症状**: Android/iOS 权限请求被拒绝

**排查步骤**:
1. 检查 manifest.json 权限配置
2. 检查权限描述文案
3. 检查系统设置中的权限状态

**解决方案**:
- Android: 在设置中手动授予权限
- iOS: 重新安装应用触发权限请求

---

## ✅ 测试完成检查清单

### 基础功能
- [ ] 用户登录
- [ ] WebSocket 连接
- [ ] 联系人列表
- [ ] 群组列表

### 消息收发
- [ ] 单聊文本消息
- [ ] 群聊文本消息
- [ ] 图片消息
- [ ] 语音消息
- [ ] 视频消息
- [ ] 文件消息

### 会话管理
- [ ] 会话列表
- [ ] 会话置顶
- [ ] 清空未读数
- [ ] 删除会话

### 消息操作
- [ ] 消息撤回
- [ ] 消息删除

### 群组管理
- [ ] 创建群组
- [ ] 添加成员
- [ ] 移除成员

---

## 📊 测试报告模板

```markdown
# IM 系统联调测试报告

**测试日期**: YYYY-MM-DD  
**测试人员**: XXX  
**测试环境**: Android/iOS/H5  

## 测试结果

| 功能模块 | 测试用例 | 测试结果 | 备注 |
|---------|---------|---------|------|
| 用户登录 | 登录功能 | ✅ 通过 | - |
| WebSocket | 连接建立 | ✅ 通过 | - |
| 消息收发 | 文本消息 | ✅ 通过 | - |
| ... | ... | ... | ... |

## 发现的问题

1. **问题描述**: XXX
   - **严重程度**: 高/中/低
   - **复现步骤**: XXX
   - **预期结果**: XXX
   - **实际结果**: XXX

## 测试总结

- 总用例数: XX
- 通过数: XX
- 失败数: XX
- 通过率: XX%

## 建议

1. XXX
2. XXX
```

---

**下一步**: 开始执行测试用例，记录测试结果，发现并修复问题。
