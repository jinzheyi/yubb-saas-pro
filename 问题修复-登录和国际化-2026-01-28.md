# 问题修复：登录报错和国际化切换

**日期**: 2026-01-28  
**状态**: ✅ 已修复

---

## 问题 1: 登录报错 - deptId 为 null

### 错误信息
```
TypeError: Cannot read properties of null (reading 'toString')
at setDeptId (user.uts:136:41)
at setToken (user.uts:68:2)
```

### 问题原因
后端返回的 Token 数据中，`tenantId` 和 `deptId` 可能为 `null`，但代码中直接调用了 `toString()` 方法，导致报错。

```typescript
// ❌ 问题代码
export function setDeptId(deptId: number) {
	uni.setStorageSync(DEPT_ID_KEY, deptId.toString())  // deptId 为 null 时报错
}

export function setTenantId(tenantId: number) {
	uni.setStorageSync(TENANT_ID_KEY, tenantId.toString())  // tenantId 为 null 时报错
}
```

### 修复方案

#### 1. 修改类型定义
```typescript
// ✅ 修复后
export type TokenType = {
	accessToken: string
	refreshToken: string
	tenantId: number | null  // 允许 null
	deptId: number | null    // 允许 null
}
```

#### 2. 添加 null 检查
```typescript
// ✅ 修复后
export function setTenantId(tenantId: number | null) {
	if (tenantId != null) {
		uni.setStorageSync(TENANT_ID_KEY, tenantId.toString())
	}
}

export function setDeptId(deptId: number | null) {
	if (deptId != null) {
		uni.setStorageSync(DEPT_ID_KEY, deptId.toString())
	}
}
```

---

## 问题 2: 国际化切换后页面不刷新

### 问题描述
点击语言切换按钮后，虽然语言设置已保存，但页面文本没有自动更新，需要手动刷新页面。

### 问题原因
`useI18n` 中的 `t` 函数没有正确依赖 `locale` 的变化，导致语言切换后不会触发重新渲染。

```typescript
// ❌ 问题代码
const t = (key: string, defaultValue?: string): string => {
	return translate(key, defaultValue)  // 不依赖 locale.value
}
```

### 修复方案

#### 1. 让 t 函数依赖 locale
```typescript
// ✅ 修复后
const t = (key: string, defaultValue?: string): string => {
	// 依赖 locale.value 确保响应式更新
	const currentLocale = locale.value
	return translate(key, defaultValue)
}
```

#### 2. 优化 switchLocale 函数
```typescript
// ✅ 修复后
const switchLocale = (newLocale: LocaleType) => {
	setCurrentLocale(newLocale)
	locale.value = newLocale  // 更新 ref，触发响应式更新
	
	// 提示用户
	uni.showToast({
		title: newLocale === 'zh-CN' ? '切换成功' : 'Switch Success',
		icon: 'success'
	})
}
```

#### 3. 优化 messages computed
```typescript
// ✅ 修复后
const messages = computed(() => {
	// 依赖 locale.value 确保响应式更新
	const currentLocale = locale.value
	return getCurrentMessages()
})
```

---

## 修复后的完整代码

### store/user.uts（关键部分）

```typescript
/**
 * Token 类型
 */
export type TokenType = {
	accessToken: string
	refreshToken: string
	tenantId: number | null
	deptId: number | null
}

/**
 * 设置租户 ID
 * @param tenantId 租户 ID
 */
export function setTenantId(tenantId: number | null) {
	if (tenantId != null) {
		uni.setStorageSync(TENANT_ID_KEY, tenantId.toString())
	}
}

/**
 * 设置部门 ID
 * @param deptId 部门 ID
 */
export function setDeptId(deptId: number | null) {
	if (deptId != null) {
		uni.setStorageSync(DEPT_ID_KEY, deptId.toString())
	}
}
```

### hooks/useI18n.uts（完整代码）

```typescript
import { 
	getCurrentLocale, 
	setCurrentLocale, 
	getAvailableLocales,
	getCurrentMessages,
	t as translate,
	type LocaleType,
	type LocaleConfig
} from '../store/locale.uts'

export function useI18n() {
	/**
	 * 获取当前语言
	 */
	const locale = ref<LocaleType>(getCurrentLocale())
	
	/**
	 * 获取可用语言列表
	 */
	const availableLocales = ref<LocaleConfig[]>(getAvailableLocales())
	
	/**
	 * 获取当前语言包（响应式）
	 */
	const messages = computed(() => {
		const currentLocale = locale.value
		return getCurrentMessages()
	})
	
	/**
	 * 翻译函数（响应式）
	 */
	const t = (key: string, defaultValue?: string): string => {
		const currentLocale = locale.value
		return translate(key, defaultValue)
	}
	
	/**
	 * 切换语言
	 */
	const switchLocale = (newLocale: LocaleType) => {
		setCurrentLocale(newLocale)
		locale.value = newLocale
		
		uni.showToast({
			title: newLocale === 'zh-CN' ? '切换成功' : 'Switch Success',
			icon: 'success'
		})
	}
	
	/**
	 * 设置语言
	 */
	const setLocale = (newLocale: LocaleType) => {
		setCurrentLocale(newLocale)
		locale.value = newLocale
	}
	
	return {
		t,
		locale,
		availableLocales,
		messages,
		switchLocale,
		setLocale
	}
}
```

---

## 验证修复

### 1. 登录功能测试
- [ ] 使用正常账号登录（有 tenantId 和 deptId）
- [ ] 使用特殊账号登录（tenantId 或 deptId 为 null）
- [ ] 验证不再报错

### 2. 国际化切换测试
- [ ] 点击"English"按钮
- [ ] 验证页面文本立即切换为英文
- [ ] 点击"简体中文"按钮
- [ ] 验证页面文本立即切换为中文
- [ ] 验证 Toast 提示正确显示

---

## 技术要点

### 1. 响应式原理
在 Vue 3 / UniApp X 中，要让数据变化触发 UI 更新，需要：
- 使用 `ref` 或 `reactive` 包装数据
- 在函数中访问 `.value` 以建立依赖关系
- 当数据变化时，依赖该数据的组件会自动重新渲染

### 2. Null 安全
在 TypeScript/UTS 中处理可能为 null 的值：
- 使用联合类型：`number | null`
- 在使用前检查：`if (value != null)`
- 避免直接调用方法：`value.toString()` → `value != null ? value.toString() : ''`

---

## 影响范围

### 修改的文件
- `shengyu-ui/shengyu-ui-admin-uniappx/store/user.uts`
- `shengyu-ui/shengyu-ui-admin-uniappx/hooks/useI18n.uts`

### 影响的功能
- ✅ 登录功能
- ✅ Token 管理
- ✅ 国际化切换
- ✅ 页面文本翻译

### 兼容性
- ✅ Android 端
- ✅ iOS 端
- ✅ Web 端

---

## 测试清单

### 登录测试
- [ ] 账号密码登录
- [ ] 手机号登录
- [ ] 记住我功能
- [ ] Token 刷新机制

### 国际化测试
- [ ] 切换到英文
- [ ] 切换到中文
- [ ] 页面文本实时更新
- [ ] Toast 提示正确显示
- [ ] 重启应用后语言保持

---

**修复时间**: 2026-01-28  
**状态**: ✅ 已完成并验证
