# IM 即时通讯系统文档

> **项目**: 圣钰 SaaS Pro - IM 即时通讯系统  
> **更新日期**: 2026-03-17

---

## 📚 文档列表

### 1. 主设计文档（必读）⭐⭐⭐
**[IM即时通讯架构设计文档-v2.0.md](./IM即时通讯架构设计文档-v2.0.md)**
- 面向企业级 IM（对齐企业微信/钉钉）的架构设计与闭环能力说明
- 统一鉴权、Presence/Lease、多端互踢、协议双栈、可靠性、离线推送、观测运维、高可用与降级

**[IM即时通讯开发任务清单-v2.0.md](./IM即时通讯开发任务清单-v2.0.md)**
- 可执行 Backlog（按 Milestone 拆分）
- 每项任务包含目标/范围/依赖/验收标准

### 2. 功能测试指南
**[功能测试指南.md](./功能测试指南.md)**
- 群文件功能测试用例
- 聊天记录搜索测试用例
- 测试步骤和验证点
- 测试结果记录表

---

## 🚀 快速开始（开发联调）

### 1) WebSocket 服务地址

- 服务端端口：`9000`（`shengyu.netty.port`）
- WebSocket Path：`/ws`（`shengyu.netty.webSocketPath`）
- 示例：`ws://localhost:9000/ws`

### 2) 协议与关键约束（务必遵循）

- **[Long/ID 精度]** JSON 场景下所有 `Long`（`messageId/sequence/chatId/groupId/userId/tenantId/...`）必须按 `string` 处理，禁止转 `number` 后参与去重/索引。
- **[端侧约束汇总]** uni-app-x/UTS 已知限制与踩坑清单：`sql/doc/uni-app-x开发资料-摘录.md`（重点看 6.1）
- **[协商顺序]** 连接建立后必须先完成 `PROBE`，再发送 `AUTH_REQ`（严格模式下未 PROBE 会被关闭）。
- **[codec 绑定]** 一个连接只绑定一个 `codec`（`json` 或 `pb`），绑定后不得切换；切换必须重连。
- **[载体约束]**
  - `codec=json`：业务消息走 TextFrame
  - `codec=pb`：除 `PROBE/PROBE_RESP/CLOSE` 外，业务消息必须走 BinaryFrame

### 3) 推荐阅读顺序

1. `IM即时通讯架构设计文档-v2.0.md`：理解协议、鉴权与可靠性闭环
2. `IM即时通讯开发任务清单-v2.0.md`：按 Milestone 推进与验收

---

## 🎯 快速导航

### 新功能开发
1. 查看主设计文档了解整体架构
2. 在主设计文档中找到相关模块
3. 参考已有实现进行开发

### 功能测试
1. 查看功能测试指南
2. 按照测试用例进行测试
3. 记录测试结果

### 问题排查
1. 查看主设计文档中的实现细节
2. 检查数据库设计和API接口
3. 查看代码实现和日志

---

## 🔄 文档管理原则

- 本目录文档必须以“当前工程代码”为准，避免保留过期的 v1 口径。
- 当协议（PROBE/AUTH/codec/ACK）、可靠性（幂等/回执/补偿）、或关键约束（Long 精度）发生变化时，需同步更新：
  - `IM即时通讯架构设计文档-v2.0.md`
  - `IM即时通讯开发任务清单-v2.0.md`

---

## 💡 使用技巧

- 需要确认“现状是什么”：优先看 `IM即时通讯架构设计文档-v2.0.md` 的“当前工程落地情况（关键落点）”。
- 需要推进开发/验收：以 `IM即时通讯开发任务清单-v2.0.md` 为唯一 backlog。

---

## 📞 联系方式

- 项目: 圣钰 SaaS Pro
- 模块: IM 即时通讯系统
- 开发团队: IM高级开发架构师