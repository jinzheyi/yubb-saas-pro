# 验证码测试指南 - 快速参考

## 🚀 快速开始（3 步）

### 步骤 1: 重启后端服务 ⚠️

```bash
# 在 IDEA 中：
# 1. 停止当前运行的 ShengyuServerApplication
# 2. 重新运行 ShengyuServerApplication
```

**重要**: 配置已修改，必须重启才能生效！

---

### 步骤 2: 测试后端接口（可选）

使用 Postman 或 curl 测试：

```bash
# 1. 先获取验证码
POST http://localhost:48080/admin-api/system/captcha/get
Content-Type: application/json

{
  "captchaType": "blockPuzzle"
}

# 响应示例：
{
  "repCode": "0000",
  "repData": {
    "originalImageBase64": "data:image/png;base64,...",
    "jigsawImageBase64": "data:image/png;base64,...",
    "token": "abc123...",
    "secretKey": "xyz789..."
  }
}

# 2. 校验验证码（使用上面返回的 token）
POST http://localhost:48080/admin-api/system/captcha/check
Content-Type: application/json

{
  "captchaType": "blockPuzzle",
  "pointJson": "{\"x\":100,\"y\":5}",
  "token": "abc123..."
}

# 预期响应：
{
  "repCode": "0000",  # ✅ 成功！
  "repMsg": "success"
}
```

**如果返回 `repCode: "0000"`，说明后端配置成功！** ✅

---

### 步骤 3: 测试 App 端

1. **运行 UniApp X 项目**
   - 在 HBuilderX 中运行到模拟器或真机

2. **打开登录页面**
   - 验证码图片应该正常显示

3. **滑动验证码**
   - 滑动滑块到正确位置
   - 观察控制台日志

4. **查看日志**

**成功的日志** ✅:
```
校验验证码请求参数: {
  captchaType: "blockPuzzle",
  pointJson: "{\"x\":150,\"y\":5}",
  token: "abc123..."
}

校验验证码响应: {
  repCode: "0000",  # ✅ 成功！
  repMsg: "success",
  repData: {
    captchaVerification: "xxx"
  }
}

登录成功！
```

**失败的日志** ❌:
```
校验验证码响应: {
  repCode: "0001",  # ❌ 失败
  repMsg: "Illegal base64 character 7b"
}
```

如果还是失败，说明后端服务没有重启或配置未生效。

---

## 🔍 问题排查

### 问题 1: 后端接口返回 `repCode: "0001"`

**原因**: 后端服务未重启或配置未生效

**解决**:
1. 确认 `application.yaml` 中有 `aes-status: false`
2. 完全停止后端服务
3. 重新启动后端服务
4. 查看启动日志，确认配置加载

---

### 问题 2: App 端验证码图片不显示

**原因**: 后端服务未启动或网络问题

**解决**:
1. 确认后端服务正在运行
2. 检查 `config/app.config.uts` 中的 `BASE_URL`
3. 确认网络连接正常

---

### 问题 3: 滑动不流畅

**原因**: 已修复，不应该再出现

**如果还有问题**:
1. 检查 `slider-captcha.uvue` 是否是最新版本
2. 查看控制台是否有错误日志

---

### 问题 4: 验证总是失败

**可能原因**:
1. 滑动位置不准确
2. 后端配置未生效
3. Token 过期

**解决**:
1. 多试几次，确保滑动到正确位置
2. 重启后端服务
3. 刷新验证码重新获取

---

## 📊 测试检查清单

### 后端检查

- [ ] `application.yaml` 中添加了 `aes-status: false`
- [ ] 后端服务已重启
- [ ] 启动日志无错误
- [ ] Postman 测试返回 `repCode: "0000"`

### App 端检查

- [ ] 验证码图片正常显示
- [ ] 滑块可以流畅滑动
- [ ] 控制台日志显示 `repCode: "0000"`
- [ ] 验证成功后可以登录

### 功能检查

- [ ] 获取验证码成功
- [ ] 滑动验证成功
- [ ] 登录成功
- [ ] 跳转到首页

---

## 🎯 预期结果

### 完整的成功流程

1. **打开登录页面**
   - 自动获取验证码
   - 图片正常显示

2. **滑动验证码**
   - 滑动流畅
   - 位置准确

3. **验证成功**
   - 控制台显示 `repCode: "0000"`
   - 自动进入登录流程

4. **登录成功**
   - 跳转到首页
   - 显示用户信息

---

## 📝 关键配置

### 后端配置（已修改）

**文件**: `shengyu-server/src/main/resources/application.yaml`

```yaml
aj:
  captcha:
    aes-status: false  # ✅ 关键配置
```

### App 端配置

**文件**: `shengyu-ui/shengyu-ui-admin-uniappx/config/app.config.uts`

```typescript
export const BASE_URL = 'http://localhost:48080'
```

确保 URL 和端口正确。

---

## 🆘 需要帮助？

### 查看详细文档

- `验证码AES加密问题-已解决.md` - 完整的解决方案
- `验证码AES加密问题-最终解决方案.md` - 技术细节
- `验证码快速配置指南.md` - 配置步骤

### 查看日志

**后端日志**:
- 查看 IDEA 控制台
- 查看 `logs/shengyu-server.log`

**App 端日志**:
- 查看 HBuilderX 控制台
- 查看浏览器开发者工具（H5）

### 常用命令

```bash
# 查看后端日志
tail -f ~/logs/shengyu-server.log

# 清除 Redis 缓存（如果需要）
redis-cli
> SELECT 1
> FLUSHDB
```

---

## ✅ 成功标志

看到以下日志，说明一切正常：

```
=== App 端控制台 ===
获取验证码成功
校验验证码响应: { repCode: "0000", repMsg: "success" }
登录成功！

=== 后端日志 ===
验证码校验成功
用户登录成功
```

---

**更新时间**: 2026-01-27  
**状态**: ✅ 配置已完成，待测试验证  

**下一步**: 重启后端服务，然后测试！
