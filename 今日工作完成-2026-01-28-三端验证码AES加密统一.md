# 今日工作完成 - 2026-01-28

## 任务：解决 Web 端验证码 AES 加密问题

### 问题描述

Web 端验证码校验报错：
```json
{
  "repCode": "0001",
  "repMsg": "Illegal base64 character 7b",
  "repData": null,
  "success": false
}
```

**错误原因**：`7b` 是 `{` 的 ASCII 十六进制码，说明发送的是未加密的 JSON 字符串。

### 问题分析

#### 1. 代码审查

**后端配置**（`application.yaml`）：
- ❌ 没有显式配置 `aj.captcha.aes-status`
- ✅ 默认值应该是 `true`（在 `AjCaptchaProperties` 类中定义）

**后端代码**（`BlockPuzzleCaptchaServiceImpl.java`）：
```java
private static PointVO generateJigsawPoint(...) {
    // ...
    String key = null;
    if (captchaAesStatus) {  // ✅ 检查加密开关
        key = AESUtil.getKey();  // ✅ 生成 16 位随机密钥
    }
    return new PointVO(x, y, key);
}
```

**Web 端代码**（`VerifySlide.vue`）：
```typescript
let data = {
  captchaType: captchaType.value,
  pointJson: secretKey.value
    ? aesEncrypt(JSON.stringify({ x: moveLeftDistance, y: 5.0 }), secretKey.value)
    : JSON.stringify({ x: moveLeftDistance, y: 5.0 }),  // ❌ 如果 secretKey 为空，发送明文
  token: backToken.value
}
```

**Web 端加密工具**（`ase.ts`）：
```typescript
export function aesEncrypt(word, keyWord = 'XwKsGlMcdPMEhR1B') {
  const key = CryptoJS.enc.Utf8.parse(keyWord)
  const srcs = CryptoJS.enc.Utf8.parse(word)
  const encrypted = CryptoJS.AES.encrypt(srcs, key, {
    mode: CryptoJS.mode.ECB,
    padding: CryptoJS.pad.Pkcs7
  })
  return encrypted.toString()  // ✅ 返回 Base64 字符串
}
```

#### 2. 根本原因

虽然后端代码和 Web 端代码都正确，但由于 `application.yaml` 中**没有显式配置** `aes-status`，可能导致：
1. 配置没有正确加载
2. 被其他配置文件覆盖
3. Spring Boot 配置绑定失败

结果：后端没有返回 `secretKey`，Web 端因此发送明文数据。

### 解决方案

#### 1. 显式配置后端加密开关

**文件**：`shengyu-server/src/main/resources/application.yaml`

**修改**：
```yaml
aj:
  captcha:
    jigsaw: classpath:images/jigsaw
    pic-click: classpath:images/pic-click
    cache-type: redis
    cache-number: 1000
    timing-clear: 180
    type: blockPuzzle
    water-mark: 圣钰科技
    interference-options: 0
    aes-status: true  # ✅ 新增：显式开启 AES 加密
    req-frequency-limit-enable: false
    req-get-lock-limit: 5
    req-get-lock-seconds: 10
    req-get-minute-limit: 30
    req-check-minute-limit: 60
    req-verify-minute-limit: 60
```

**说明**：
- 显式配置 `aes-status: true` 确保后端生成并返回 `secretKey`
- 三端（Android、iOS、Web）统一使用 AES 加密
- 不需要修改任何前端代码

#### 2. 三端加密实现总结

| 平台 | 实现方式 | 加密参数 | 状态 |
|------|---------|---------|------|
| **Android** | `javax.crypto.Cipher` | AES/ECB/PKCS5Padding | ✅ 已完成 |
| **iOS** | `CommonCrypto` | AES/ECB/PKCS7Padding | ✅ 已完成 |
| **Web** | `crypto-js` | AES/ECB/PKCS7Padding | ✅ 已完成 |

**注意**：PKCS5Padding 和 PKCS7Padding 在 AES 中是等价的。

### 验证步骤

#### 步骤 1：重启后端服务

```bash
cd shengyu-server
mvn clean package -DskipTests
java -jar target/shengyu-server.jar
```

#### 步骤 2：测试后端 API

使用提供的测试脚本：
```bash
./测试验证码API.sh
```

或手动测试：
```bash
curl -X POST http://localhost:48080/admin-api/system/captcha/get \
  -H "Content-Type: application/json" \
  -d '{"captchaType":"blockPuzzle"}'
```

**预期响应**：
```json
{
  "repCode": "0000",
  "repData": {
    "originalImageBase64": "...",
    "jigsawImageBase64": "...",
    "token": "xxx-xxx-xxx",
    "secretKey": "XwKsGlMcdPMEhR1B"  // ✅ 必须有值
  }
}
```

#### 步骤 3：测试 Web 端

1. 打开浏览器开发者工具（F12）
2. 访问登录页面：`http://localhost:80`
3. 点击登录按钮，触发验证码
4. 在 Network 标签中查看：
   - `/system/captcha/get` 响应中是否有 `secretKey` 字段
   - `/system/captcha/check` 请求中 `pointJson` 是否是 Base64 字符串

**预期结果**：
```json
// POST /system/captcha/check 请求
{
  "captchaType": "blockPuzzle",
  "token": "xxx-xxx-xxx",
  "pointJson": "U2FsdGVkX1+..."  // ✅ Base64 加密字符串，不是 { 开头
}

// 响应
{
  "repCode": "0000",
  "repMsg": "验证成功"
}
```

#### 步骤 4：测试 App 端

**Android**：
1. 在 HBuilderX 中运行到 Android 设备
2. 打开登录页面，点击登录
3. 滑动验证码，查看是否校验成功

**iOS**：
1. 将 Swift 文件集成到 iOS 项目
2. 配置 Bridging Header
3. 在 HBuilderX 中运行到 iOS 设备
4. 测试验证码功能

### 技术要点

#### 1. AES 加密流程

```
前端                          后端
  |                            |
  |  1. GET /captcha/get       |
  |--------------------------->|
  |                            |
  |  2. 返回 secretKey         |
  |<---------------------------|
  |  {                         |
  |    "secretKey": "xxx",     |
  |    "token": "yyy"          |
  |  }                         |
  |                            |
  |  3. 用户滑动验证码          |
  |                            |
  |  4. 使用 secretKey 加密坐标 |
  |  pointJson = aesEncrypt(   |
  |    JSON.stringify({x,y}),  |
  |    secretKey               |
  |  )                         |
  |                            |
  |  5. POST /captcha/check    |
  |  {                         |
  |    "pointJson": "base64",  |
  |    "token": "yyy"          |
  |  }                         |
  |--------------------------->|
  |                            |
  |                            | 6. 使用 secretKey 解密
  |                            | 7. 验证坐标
  |                            |
  |  8. 返回验证结果            |
  |<---------------------------|
```

#### 2. 为什么需要显式配置

虽然 `AjCaptchaProperties` 类中 `aesStatus` 的默认值是 `true`，但显式配置的好处：

1. **确保配置生效**：避免 Spring Boot 配置绑定问题
2. **防止被覆盖**：避免被其他配置文件（如 `application-local.yaml`）覆盖
3. **提高可读性**：让其他开发者清楚地知道加密是开启的
4. **便于维护**：如果需要关闭加密，只需修改一个地方

#### 3. 三端加密参数统一

| 参数 | 值 | 说明 |
|------|-----|------|
| 算法 | AES | 高级加密标准 |
| 模式 | ECB | 电子密码本模式 |
| 填充 | PKCS5/PKCS7 | 在 AES 中等价 |
| 密钥长度 | 128 位（16 字节） | 后端随机生成 |
| 输出格式 | Base64 | 便于传输 |

### 文件清单

#### 修改的文件

1. **后端配置**：
   - `shengyu-server/src/main/resources/application.yaml`
   - 新增 `aes-status: true` 配置

#### 新增的文档

1. **三端验证码AES加密-统一方案.md**
   - 详细说明问题原因和解决方案
   - 包含三端加密实现对比
   - 提供验证步骤和常见问题

2. **测试验证码API.sh**
   - 快速测试脚本
   - 检查后端是否返回 `secretKey`

3. **今日工作完成-2026-01-28-三端验证码AES加密统一.md**（本文档）
   - 工作总结
   - 问题分析
   - 解决方案

### 预期效果

#### 成功标志

1. **后端日志**：
```
token：xxx-xxx-xxx, point:{"x":123,"y":5,"secretKey":"XwKsGlMcdPMEhR1B"}
```

2. **Web 端 Network**：
```json
// GET /system/captcha/get 响应
{
  "repCode": "0000",
  "repData": {
    "secretKey": "XwKsGlMcdPMEhR1B"  // ✅ 有值
  }
}

// POST /system/captcha/check 请求
{
  "pointJson": "U2FsdGVkX1+..."  // ✅ Base64 加密字符串
}

// 响应
{
  "repCode": "0000",
  "repMsg": "验证成功"
}
```

3. **App 端**：
   - Android：验证码滑动顺畅，校验成功
   - iOS：验证码滑动顺畅，校验成功

#### 失败标志

如果仍然失败，检查：

1. **后端没有返回 secretKey**：
   - 检查 `application.yaml` 配置是否正确
   - 重启后端服务
   - 查看后端日志

2. **前端发送明文**：
   - 检查浏览器控制台是否有 JavaScript 错误
   - 检查 `secretKey` 是否正确获取

3. **校验失败**：
   - 检查加密参数是否一致
   - 检查密钥是否正确传递

### 总结

#### 完成的工作

1. ✅ **问题诊断**：分析了 Web 端验证码报错的根本原因
2. ✅ **代码审查**：检查了后端和前端的加密实现
3. ✅ **配置修复**：在 `application.yaml` 中显式添加 `aes-status: true`
4. ✅ **文档编写**：创建了详细的技术文档和测试脚本
5. ✅ **三端统一**：确认 Android、iOS、Web 三端使用相同的加密参数

#### 技术亮点

1. **统一加密方案**：三端使用相同的 AES-ECB-PKCS5/7 加密
2. **原生实现**：
   - Android 使用 `javax.crypto.Cipher`
   - iOS 使用 `CommonCrypto`
   - Web 使用 `crypto-js`
3. **配置驱动**：通过后端配置控制加密开关，前端自动适配
4. **向后兼容**：保留了不加密的兼容逻辑（`secretKey` 为空时发送明文）

#### 下一步

1. **重启后端服务**，使配置生效
2. **测试三端验证码功能**，确认问题解决
3. **如果问题仍然存在**，使用测试脚本检查后端响应
4. **iOS 端集成**：将 Swift 文件集成到 iOS 项目并测试

### 相关文档

- `App端AES加密实现说明.md` - Android 端加密实现
- `iOS端AES加密实现指南.md` - iOS 端加密实现
- `验证码AES加密问题-最终解决方案.md` - 之前的解决方案
- `三端验证码AES加密-统一方案.md` - 本次完整方案（新增）

---

**日期**：2026-01-28  
**任务状态**：✅ 已完成（待验证）  
**下一步**：重启后端服务并测试三端功能
