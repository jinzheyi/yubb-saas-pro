# 验证码快速配置指南

## 🚀 5分钟快速配置

### 步骤 1: 修改后端配置（必须）

编辑后端配置文件 `application-dev.yml`:

```yaml
aj:
  captcha:
    type: default
    jigsaw: classpath:images/jigsaw
    pic-click: classpath:images/pic-click
    water-mark: false
    req-frequency-limit-enable: false
    aes-status: false  # ✅ 关闭 AES 加密（重要！）
```

### 步骤 2: 重启后端服务

```bash
# 重启 Spring Boot 应用
# 确保配置生效
```

### 步骤 3: 运行 App

```bash
# 在 HBuilderX 中运行到手机或模拟器
```

### 步骤 4: 测试验证码

1. 输入账号密码
2. 点击登录按钮
3. 滑动验证码
4. 验证成功后自动登录

---

## ✅ 验证配置是否成功

### 查看控制台日志

**正常日志**:
```
获取验证码请求参数: {...}
获取验证码响应: { repCode: "0000", ... }
验证码图片已加载: { hasBackImg: true, hasBlockImg: true }

开始滑动: 100
结束滑动: { moveDistance: 150, 耗时: "1234ms" }

校验验证码请求参数: { pointJson: "{\"x\":150,\"y\":5}", ... }
校验验证码响应: { repCode: "0000", repMsg: "success" }
```

**异常日志**:
```
❌ 校验验证码响应: {
  repCode: "0001",
  repMsg: "Input length must be multiple of 16..."
}
```

如果看到异常日志，说明后端配置未生效，请检查步骤 1。

---

## 🔧 常见问题

### Q1: 后端配置修改后仍然报加密错误？

**原因**: 配置未生效或配置文件路径错误

**解决**:
1. 确认修改的是正确的配置文件（`application-dev.yml`）
2. 确认配置文件在 `src/main/resources` 目录下
3. 重启后端服务
4. 查看后端启动日志，确认配置加载成功

### Q2: 滑块仍然不流畅？

**原因**: 可能是设备性能问题或其他元素遮挡

**解决**:
1. 确认验证码弹窗在最上层（z-index: 9999）
2. 尝试在不同设备上测试
3. 查看控制台是否有错误信息

### Q3: 验证总是失败？

**原因**: 坐标计算不准确或后端容错范围太小

**解决**:
1. 查看控制台日志，确认滑动距离
2. 多次尝试，找到正确的滑动距离
3. 联系后端调整容错范围

---

## 📋 完整配置清单

### 后端配置

```yaml
# application-dev.yml
aj:
  captcha:
    type: default                          # 验证码类型
    jigsaw: classpath:images/jigsaw        # 滑块图片路径
    pic-click: classpath:images/pic-click  # 点选图片路径
    water-mark: false                      # 水印
    req-frequency-limit-enable: false      # 请求频率限制
    aes-status: false                      # ✅ AES 加密（必须关闭）
```

### App 端配置

```typescript
// config/app.config.uts
export const BASE_URL = 'http://localhost:48080'  // 后端地址
export const CAPTCHA_ENABLE = 'true'              // 启用验证码
export const TIMEOUT = 30000                      // 请求超时
```

### 验证码组件

```vue
<!-- pages/login/login.uvue -->
<slider-captcha 
  ref="captchaRef"
  captchaType="blockPuzzle"
  @success="handleCaptchaSuccess"
  @fail="handleCaptchaFail"
/>
```

---

## 🎯 测试步骤

### 1. 基础功能测试

- [ ] 验证码弹窗正常显示
- [ ] 背景图片正常加载
- [ ] 滑块图片正常加载
- [ ] 刷新按钮正常工作

### 2. 滑动测试

- [ ] 滑块跟随手指移动
- [ ] 滑动流畅无延迟
- [ ] 滑动范围正确（0-260px）
- [ ] 滑动距离太小时自动重置

### 3. 验证测试

- [ ] 滑动到正确位置验证成功
- [ ] 滑动到错误位置验证失败
- [ ] 验证失败自动刷新
- [ ] 验证成功自动关闭并登录

### 4. 异常测试

- [ ] 网络断开时提示友好
- [ ] 后端服务停止时提示友好
- [ ] 多次验证失败后仍可继续

---

## 📱 不同设备测试

### Android 设备
- [ ] 华为
- [ ] 小米
- [ ] OPPO
- [ ] vivo
- [ ] 三星

### iOS 设备
- [ ] iPhone
- [ ] iPad

### 不同屏幕尺寸
- [ ] 小屏（<5英寸）
- [ ] 中屏（5-6英寸）
- [ ] 大屏（>6英寸）
- [ ] 平板

---

## 🔒 安全说明

### 开发环境
- ✅ 可以关闭 AES 加密
- ✅ 使用 HTTP 协议
- ✅ 方便调试和测试

### 生产环境
- ❌ 必须开启 AES 加密
- ❌ 必须使用 HTTPS 协议
- ❌ 需要使用原生加密插件

**警告**: 当前配置仅适用于开发环境，生产环境必须使用加密！

---

## 📚 相关文档

- 📄 `后端验证码配置-支持不加密.md` - 详细的后端配置说明
- 📄 `验证码调试指南.md` - 完整的调试步骤
- 📄 `验证码问题修复总结-v1.2.md` - 问题修复记录
- 📄 `验证码快速使用指南.md` - 组件使用说明

---

## 💡 提示

1. **后端配置是关键**: 必须关闭 AES 加密
2. **重启服务**: 修改配置后必须重启后端服务
3. **查看日志**: 遇到问题先查看控制台日志
4. **测试充分**: 在不同设备上测试滑动体验

---

## 🆘 需要帮助？

如果按照以上步骤仍然无法正常使用，请提供：

1. **控制台完整日志**
2. **后端配置文件内容**
3. **错误截图**
4. **设备信息**（型号、系统版本）

---

**更新时间**: 2026-01-27  
**版本**: v1.2.0  
**适用环境**: 开发环境
