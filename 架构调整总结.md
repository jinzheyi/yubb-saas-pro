# 即时通讯中间件架构调整总结

## 一、调整背景

根据您的反馈，中间件应该只提供底层能力（连接管理、协议处理、消息路由等），而具体的业务数据存储应该由业务模块（system/platform）自己实现。

## 二、调整内容

### 2.1 删除的文件

从中间件中删除了以下业务相关的文件：

1. ❌ `ImMessageDO.java` - 消息 DO 对象（应由业务模块定义）
2. ❌ `ImMessageMapper.java` - 消息 Mapper（应由业务模块实现）
3. ❌ `MessageStorageServiceImpl.java` - 消息存储具体实现（应由业务模块实现）
4. ❌ `MessageCacheServiceImpl.java` - 消息缓存具体实现（应由业务模块实现）

### 2.2 新增的文件

#### 中间件层（SPI 接口）

1. ✅ `MessageStorageService.java` - 重新设计为纯 SPI 接口
   ```java
   public interface MessageStorageService {
       void saveMessage(ImMessage message);
       Long saveMessageWithId(ImMessage message);
   }
   ```

2. ✅ `MessageCacheService.java` - 简化为核心缓存接口
   ```java
   public interface MessageCacheService {
       void cacheUnreadCount(Long userId, long count);
       Long getCachedUnreadCount(Long userId);
       long incrementUnreadCount(Long userId, long delta);
   }
   ```

3. ✅ `NoOpMessageStorageServiceImpl.java` - 空实现（默认）
   - 当业务模块未提供实现时使用
   - 只打印日志，不做实际存储

4. ✅ `NoOpMessageCacheServiceImpl.java` - 空实现（默认）
   - 当业务模块未提供实现时使用
   - 不影响核心功能

#### 示例代码（examples/）

1. ✅ `im_message_example.sql` - 数据库表结构示例
2. ✅ `SystemImMessageDO.java.example` - DO 对象示例
3. ✅ `SystemImMessageMapper.java.example` - Mapper 示例
4. ✅ `SystemMessageStorageServiceImpl.java.example` - 服务实现示例

#### 文档

1. ✅ `ARCHITECTURE.md` - 详细的架构设计文档
2. ✅ `README.md` - 更新为 SPI 架构说明
3. ✅ `QUICK-START.md` - 更新集成步骤

### 2.3 修改的文件

1. ✅ `NettyAutoConfiguration.java` - 更新自动配置
   - 使用 `@ConditionalOnMissingBean` 允许业务模块覆盖
   - 默认注册空实现（NoOp）
   - 添加警告日志提示业务模块实现

2. ✅ 消息处理器（TextMessageProcessor 等）
   - 移除对具体 DO 的依赖
   - 直接调用 SPI 接口

## 三、新架构设计

### 3.1 职责划分

```
┌─────────────────────────────────────────────────────────┐
│           中间件（shengyu-spring-boot-starter-websocket）  │
│                                                           │
│  【底层能力】                                              │
│  ✅ 连接管理（Netty 服务器、会话管理）                      │
│  ✅ 协议处理（WebSocket、Protobuf）                        │
│  ✅ 消息路由（消息分发、消息转发）                          │
│  ✅ 认证鉴权（Token 验证）                                 │
│                                                           │
│  【SPI 接口】                                              │
│  - MessageStorageService（消息存储）                      │
│  - MessageCacheService（消息缓存）                        │
│  - OfflinePushService（离线推送）                         │
└─────────────────────────────────────────────────────────┘
                          ▲
                          │ 实现接口
                          │
        ┌─────────────────┴─────────────────┐
        │                                   │
┌───────▼────────┐              ┌──────────▼─────────┐
│  system 模块    │              │  platform 模块      │
│                │              │                    │
│ 【业务实现】    │              │ 【业务实现】        │
│ ✅ 消息存储     │              │ ✅ 消息存储         │
│ ✅ 业务逻辑     │              │ ✅ 业务逻辑         │
│ ✅ 缓存策略     │              │ ✅ 缓存策略         │
│ ✅ 推送集成     │              │ ✅ 推送集成         │
│                │              │                    │
│ 【数据库】      │              │ 【数据库】          │
│ - system_im_*  │              │ - platform_im_*    │
└────────────────┘              └────────────────────┘
```

### 3.2 数据流向

**消息发送流程**：
```
客户端
  │
  ▼
中间件（Netty）
  │ 1. 接收消息
  │ 2. 协议解析
  ▼
中间件（MessageProcessor）
  │ 3. 调用 MessageStorageService.saveMessage()
  ▼
业务模块（SystemMessageStorageServiceImpl）
  │ 4. 转换为 DO 对象
  │ 5. 保存到 system_im_message 表
  │ 6. 更新会话、未读数等业务逻辑
  ▼
中间件（MessageProcessor）
  │ 7. 转发给接收者
  ▼
接收者客户端
```

## 四、业务模块集成步骤

### 4.1 system 模块集成

#### 步骤 1：创建数据库表

```sql
CREATE TABLE system_im_message (
    id BIGINT PRIMARY KEY,
    message_type INT NOT NULL,
    sender_id BIGINT NOT NULL,
    receiver_id BIGINT,
    tenant_id BIGINT NOT NULL,  -- 租户隔离
    body TEXT,
    status INT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_receiver_status (receiver_id, status),
    INDEX idx_tenant (tenant_id)
);
```

#### 步骤 2：创建 DO 对象

```java
@TableName("system_im_message")
@Data
public class SystemImMessageDO extends BaseDO {
    @TableId(type = IdType.INPUT)
    private Long id;
    private Integer messageType;
    private Long senderId;
    private Long receiverId;
    private Long tenantId;  // 租户ID
    private String body;
    private Integer status;
}
```

#### 步骤 3：创建 Mapper

```java
@Mapper
public interface SystemImMessageMapper extends BaseMapperX<SystemImMessageDO> {
}
```

#### 步骤 4：实现 SPI 接口

```java
@Service
public class SystemMessageStorageServiceImpl implements MessageStorageService {
    
    @Autowired
    private SystemImMessageMapper messageMapper;
    
    @Override
    public void saveMessage(ImMessage message) {
        SystemImMessageDO messageDO = convertToMessageDO(message);
        messageMapper.insert(messageDO);
        // 其他业务逻辑...
    }
}
```

### 4.2 platform 模块集成

#### 步骤 1：创建数据库表

```sql
CREATE TABLE platform_im_message (
    id BIGINT PRIMARY KEY,
    message_type INT NOT NULL,
    sender_id BIGINT NOT NULL,
    receiver_id BIGINT,
    -- 注意：platform 不需要 tenant_id
    body TEXT,
    status INT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_receiver_status (receiver_id, status)
);
```

#### 步骤 2-4：类似 system 模块

不同之处：
- 表名：`platform_im_message`
- 无需 `tenantId` 字段
- 可以有不同的业务逻辑

## 五、优势总结

### 5.1 架构优势

✅ **职责清晰**：中间件只负责底层能力，业务逻辑由业务模块实现  
✅ **灵活扩展**：不同业务模块可以有不同的实现策略  
✅ **独立演进**：中间件和业务模块可以独立升级  
✅ **多租户支持**：system 和 platform 可以有不同的表结构和业务逻辑  

### 5.2 技术优势

✅ **高性能**：Netty + Protobuf + Redis 缓存  
✅ **高可用**：支持集群部署、负载均衡  
✅ **易维护**：代码分层清晰，职责单一  
✅ **易测试**：接口驱动，便于单元测试  

### 5.3 业务优势

✅ **业务自主**：业务模块完全控制数据存储和业务逻辑  
✅ **灵活定制**：可以根据业务需求定制功能  
✅ **数据隔离**：不同业务模块的数据完全隔离  
✅ **安全可控**：业务数据不经过中间件，更安全  

## 六、对比表

| 项目 | 调整前 | 调整后 |
|------|--------|--------|
| **中间件职责** | 连接管理 + 消息存储 | 仅连接管理 |
| **DO 对象** | 中间件定义 | 业务模块定义 |
| **Mapper** | 中间件实现 | 业务模块实现 |
| **表结构** | 统一表结构 | 各模块自定义 |
| **业务逻辑** | 中间件实现 | 业务模块实现 |
| **数据隔离** | 通过 tenant_id | 物理隔离（不同表） |
| **扩展性** | 受限 | 灵活 |
| **维护性** | 耦合 | 解耦 |

## 七、迁移指南

如果之前已经使用了旧版本，迁移步骤：

### 7.1 中间件升级

1. 删除对 `ImMessageDO`、`ImMessageMapper` 的引用
2. 更新依赖到新版本

### 7.2 业务模块改造

1. 在业务模块创建自己的 DO、Mapper
2. 实现 `MessageStorageService` 接口
3. 注册为 Spring Bean（`@Service`）

### 7.3 数据迁移

如果需要迁移数据：
```sql
-- 从旧表迁移到新表
INSERT INTO system_im_message 
SELECT * FROM im_message WHERE tenant_id IS NOT NULL;

INSERT INTO platform_im_message 
SELECT id, message_type, sender_id, receiver_id, body, status, create_time 
FROM im_message WHERE tenant_id IS NULL;
```

## 八、注意事项

### 8.1 必须实现的接口

- ✅ **MessageStorageService**：必须实现，否则消息不会被持久化
  - 中间件会打印警告日志
  - 使用空实现（NoOp）不会影响核心功能

### 8.2 可选实现的接口

- ⚪ **MessageCacheService**：可选，不实现则使用空实现
- ⚪ **OfflinePushService**：可选，不实现则不支持离线推送

### 8.3 性能优化建议

1. **数据库优化**：
   - 按租户分表（system 模块）
   - 按时间分表
   - 添加合适的索引

2. **缓存优化**：
   - 实现 MessageCacheService
   - 缓存热点数据
   - 使用 Redis 集群

3. **消息优化**：
   - 使用 Protobuf 减少带宽
   - 批量操作减少数据库压力
   - 异步处理耗时操作

## 九、总结

通过这次架构调整，我们实现了：

1. ✅ **中间件纯粹化**：只提供底层能力，不涉及业务逻辑
2. ✅ **业务模块自主化**：完全控制数据存储和业务逻辑
3. ✅ **架构清晰化**：职责明确，边界清晰
4. ✅ **扩展灵活化**：支持不同业务模块的不同需求

这是一个更加合理、更加灵活、更加易于维护的架构设计！

---

**调整时间**：2026年1月27日  
**版本**：v1.0.0 → v2.0.0  
**作者**：圣钰科技
