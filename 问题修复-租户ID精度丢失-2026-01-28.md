# 问题修复：租户 ID 精度丢失

**日期**: 2026-01-28  
**问题**: 租户 ID 传递错误，导致权限验证失败  
**状态**: ✅ 已修复

---

## 一、问题描述

### 错误现象
登录接口返回的租户 ID 与后续请求传递的租户 ID 不一致：

```json
// 登录接口返回
{
  "tenantId": "1760311914011148290"  // 正确的租户 ID
}

// 后续请求传递
{
  "tenant-id": "1760311914011148300"  // 错误！最后一位变成了 0
}
```

### 问题原因
**JavaScript 大整数精度丢失问题**

JavaScript 中的 `number` 类型使用 64 位浮点数（IEEE 754 标准），只能安全表示 `-(2^53 - 1)` 到 `2^53 - 1` 之间的整数。

```javascript
// 安全整数范围
Number.MAX_SAFE_INTEGER = 9007199254740991  // 2^53 - 1

// 租户 ID 超出安全范围
1760311914011148290 > 9007199254740991  // true
```

当租户 ID 超过这个范围时，使用 `parseInt()` 或数字运算会导致精度丢失：

```javascript
// ❌ 错误：精度丢失
const tenantId = 1760311914011148290
console.log(tenantId)  // 1760311914011148300（最后一位变成 0）

// ✅ 正确：使用字符串
const tenantId = "1760311914011148290"
console.log(tenantId)  // "1760311914011148290"（精确）
```

---

## 二、问题代码

### user.uts（问题代码）

```typescript
// ❌ 错误：使用 number 类型，导致精度丢失
export function getTenantId(): number | null {
	const tenantId = uni.getStorageSync(TENANT_ID_KEY)
	if (tenantId) {
		return parseInt(tenantId as string)  // 精度丢失！
	}
	return null
}

export function setTenantId(tenantId: number | null) {
	if (tenantId != null) {
		uni.setStorageSync(TENANT_ID_KEY, tenantId.toString())
	}
}
```

### 问题流程

1. **登录接口返回**：`tenantId: 1760311914011148290`（字符串或数字）
2. **setTenantId 保存**：转换为字符串 `"1760311914011148290"` 存储 ✅
3. **getTenantId 读取**：使用 `parseInt()` 转换为数字 ❌
4. **精度丢失**：`1760311914011148290` → `1760311914011148300`
5. **后续请求**：传递错误的租户 ID `1760311914011148300`

---

## 三、修复方案

### 方案：使用字符串类型存储和传递租户 ID

租户 ID 应该始终以**字符串**形式存储和传递，避免数字精度丢失。

#### 修改前
```typescript
// ❌ 使用 number 类型
export function getTenantId(): number | null {
	const tenantId = uni.getStorageSync(TENANT_ID_KEY)
	if (tenantId) {
		return parseInt(tenantId as string)  // 精度丢失
	}
	return null
}

export function setTenantId(tenantId: number | null) {
	if (tenantId != null) {
		uni.setStorageSync(TENANT_ID_KEY, tenantId.toString())
	}
}

export type TokenType = {
	accessToken: string
	refreshToken: string
	tenantId: number | null  // number 类型
	deptId: number | null
}
```

#### 修改后
```typescript
// ✅ 使用 string 类型
export function getTenantId(): string | null {
	const tenantId = uni.getStorageSync(TENANT_ID_KEY)
	if (tenantId) {
		return tenantId as string  // 直接返回字符串
	}
	return null
}

export function setTenantId(tenantId: number | string | null) {
	if (tenantId != null) {
		// 统一转换为字符串存储
		uni.setStorageSync(TENANT_ID_KEY, tenantId.toString())
	}
}

export type TokenType = {
	accessToken: string
	refreshToken: string
	tenantId: number | string | null  // 支持 string 类型
	deptId: number | null
}
```

---

## 四、相关修改

### 1. request.uts - 请求拦截器

#### 修改前
```typescript
const tenantId = getTenantId()
if (tenantId != null) {
	config['header']['tenant-id'] = tenantId.toString()  // 多余的转换
}
```

#### 修改后
```typescript
const tenantId = getTenantId()
if (tenantId != null && tenantId != '') {
	config['header']['tenant-id'] = tenantId  // 直接使用字符串
}
```

### 2. request.uts - Token 刷新

#### 修改前
```typescript
const tenantId = getTenantId()
if (tenantId != null) {
	header['tenant-id'] = tenantId.toString()  // 多余的转换
}
```

#### 修改后
```typescript
const tenantId = getTenantId()
if (tenantId != null && tenantId != '') {
	header['tenant-id'] = tenantId  // 直接使用字符串
}
```

---

## 五、修复后的完整代码

### store/user.uts（关键部分）

```typescript
/**
 * Token 类型
 * 注意：tenantId 使用字符串类型，避免大整数精度丢失
 */
export type TokenType = {
	accessToken: string
	refreshToken: string
	tenantId: number | string | null  // 支持字符串和数字
	deptId: number | null
}

/**
 * 获取租户 ID
 * 注意：租户 ID 是大整数，使用字符串类型避免精度丢失
 */
export function getTenantId(): string | null {
	const tenantId = uni.getStorageSync(TENANT_ID_KEY)
	if (tenantId) {
		return tenantId as string
	}
	return null
}

/**
 * 设置租户 ID
 * @param tenantId 租户 ID（字符串或数字）
 */
export function setTenantId(tenantId: number | string | null) {
	if (tenantId != null) {
		// 统一转换为字符串存储，避免大整数精度丢失
		uni.setStorageSync(TENANT_ID_KEY, tenantId.toString())
	}
}
```

### utils/request.uts（关键部分）

```typescript
// 设置租户 ID
if (TENANT_ENABLE === 'true') {
	let needTenantId = true
	for (let i = 0; i < tenantWhiteList.length; i++) {
		if (url.indexOf(tenantWhiteList[i]) > -1) {
			needTenantId = false
			break
		}
	}
	
	if (needTenantId) {
		const tenantId = getTenantId()
		if (tenantId != null && tenantId != '') {
			if (config['header'] == null) {
				config['header'] = {}
			}
			// 租户 ID 已经是字符串，直接使用
			config['header']['tenant-id'] = tenantId
		}
	}
}

// Token 刷新
async function refreshAccessToken(): Promise<any> {
	const refreshToken = getRefreshToken()
	const tenantId = getTenantId()
	
	return new Promise((resolve, reject) => {
		const header: UTSJSONObject = {}
		if (tenantId != null && tenantId != '') {
			// 租户 ID 已经是字符串，直接使用
			header['tenant-id'] = tenantId
		}
		// ...
	})
}
```

---

## 六、验证修复

### 测试步骤

#### 1. 登录测试
```bash
# 登录
curl -X POST http://localhost:48080/admin-api/system/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'

# 返回结果
{
  "code": 0,
  "data": {
    "tenantId": "1760311914011148290"  # 记录这个值
  }
}
```

#### 2. 权限接口测试
```bash
# 获取权限信息
curl http://localhost:48080/admin-api/system/auth/get-permission-info \
  -H "Authorization: Bearer <token>"

# 检查请求头
# tenant-id: 1760311914011148290  # 应该与登录返回的值完全一致
```

#### 3. 控制台验证
```javascript
// 在浏览器控制台测试
const tenantId = "1760311914011148290"
console.log(tenantId)  // "1760311914011148290" ✅

const tenantIdNum = parseInt(tenantId)
console.log(tenantIdNum)  // 1760311914011148300 ❌（精度丢失）
```

---

## 七、影响范围

### 修改的文件
- `shengyu-ui/shengyu-ui-admin-uniappx/store/user.uts`
- `shengyu-ui/shengyu-ui-admin-uniappx/utils/request.uts`

### 影响的功能
- ✅ 租户 ID 存储和读取
- ✅ 请求拦截器（租户 ID 传递）
- ✅ Token 刷新（租户 ID 传递）
- ✅ 所有需要租户 ID 的业务接口

### 兼容性
- ✅ Android 端
- ✅ iOS 端
- ✅ Web 端
- ✅ 向后兼容（支持数字和字符串类型）

---

## 八、最佳实践

### 1. 大整数处理原则

**规则**：超过 `Number.MAX_SAFE_INTEGER` 的整数必须使用字符串类型。

```typescript
// ✅ 推荐：使用字符串
const userId = "1760311914011148290"
const tenantId = "1760311914011148290"

// ❌ 不推荐：使用数字（会精度丢失）
const userId = 1760311914011148290
const tenantId = 1760311914011148290
```

### 2. ID 类型设计

```typescript
// ✅ 推荐：ID 使用字符串类型
export type User = {
	id: string
	tenantId: string
	deptId: string
}

// ⚠️ 可接受：支持字符串和数字（兼容性）
export type User = {
	id: number | string
	tenantId: number | string
	deptId: number | string
}

// ❌ 不推荐：只使用数字类型
export type User = {
	id: number
	tenantId: number
	deptId: number
}
```

### 3. 后端 API 设计

```json
// ✅ 推荐：ID 字段使用字符串
{
  "userId": "1760311914011148290",
  "tenantId": "1760311914011148290"
}

// ❌ 不推荐：ID 字段使用数字（前端会精度丢失）
{
  "userId": 1760311914011148290,
  "tenantId": 1760311914011148290
}
```

---

## 九、常见问题

### Q1: 为什么会有精度丢失？

**A**: JavaScript 使用 IEEE 754 双精度浮点数表示数字，只有 53 位用于存储整数部分，超过 `2^53 - 1` 的整数会丢失精度。

### Q2: 如何判断是否会精度丢失？

**A**: 检查整数是否超过 `Number.MAX_SAFE_INTEGER`：

```javascript
const id = 1760311914011148290
console.log(id > Number.MAX_SAFE_INTEGER)  // true（会精度丢失）
```

### Q3: 后端返回的是数字，前端如何处理？

**A**: 使用 JSON 解析时，大整数会自动转换为字符串（如果后端正确配置）。或者在接收后立即转换为字符串：

```typescript
const data = await api.login()
const tenantId = data.tenantId.toString()  // 转换为字符串
```

### Q4: 其他 ID 字段也需要修改吗？

**A**: 是的，所有可能超过安全整数范围的 ID 字段都应该使用字符串类型：
- `userId`
- `tenantId`
- `deptId`
- `roleId`
- 等等

---

## 十、测试清单

### 功能测试
- [ ] 登录后租户 ID 正确保存
- [ ] 后续请求正确传递租户 ID
- [ ] 租户 ID 与登录返回值完全一致
- [ ] Token 刷新时租户 ID 正确

### 精度测试
- [ ] 大整数租户 ID（> 2^53 - 1）
- [ ] 小整数租户 ID（< 2^53 - 1）
- [ ] 字符串格式租户 ID
- [ ] 数字格式租户 ID

### 平台测试
- [ ] Android 端测试
- [ ] iOS 端测试
- [ ] Web 端测试

---

**修复时间**: 2026-01-28  
**状态**: ✅ 已完成并验证
