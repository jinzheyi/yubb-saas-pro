class DirectConversationRef {
  const DirectConversationRef({
    required this.chatId,
    required this.targetId,
    required this.title,
  });

  final String chatId;
  final String targetId;
  final String title;
}
