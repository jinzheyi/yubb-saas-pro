<template>
	<view class="page">
		<view class="status-bar"></view>
		
		<!-- 个人信息头部 -->
		<view class="user-header">
			<view class="user-info-row">
				<image class="avatar" src="/static/avatar.jpg"></image>
				<view class="user-detail">
					<view class="name-row">
						<text class="name">用户名称</text>
						<text class="iconfont qr-icon">&#xeaf1;</text>
					</view>
				</view>
			</view>
			<view class="dept-row">
				<text class="dept-text">软件研发事业部 | 开发工程师</text>
			</view>
			<view class="status-row" @click="handleAction('status')">
				<text class="status-text">{{ t('profile.user.status') }}</text>
			</view>
		</view>

		<scroll-view class="content" scroll-y="true" show-scrollbar="false">
			<view class="menu-group">
				<view class="menu-item" @click="handleAction('theme')">
					<view class="menu-icon-wrap" style="background-color: #06b6d4;">
						<text class="iconfont menu-icon">&#xe984;</text>
					</view>
					<text class="menu-label">{{ t('profile.menu.theme') }}</text>
					<text class="iconfont arrow-icon">&#xeb03;</text>
					<view class="menu-item-border"></view>
				</view>
				<view class="menu-item" @click="handleAction('fav')">
					<view class="menu-icon-wrap" style="background-color: #fb923c;">
						<text class="iconfont menu-icon">&#xea90;</text>
					</view>
					<text class="menu-label">{{ t('profile.menu.fav') }}</text>
					<text class="iconfont arrow-icon">&#xeb03;</text>
					<view class="menu-item-border"></view>
				</view>
				<view class="menu-item" @click="handleAction('scan')">
					<view class="menu-icon-wrap" style="background-color: #f97316;">
						<text class="iconfont menu-icon">&#xea8b;</text>
					</view>
					<text class="menu-label">{{ t('profile.menu.scan') }}</text>
					<text class="iconfont arrow-icon">&#xeb03;</text>
					<view class="menu-item-border"></view>
				</view>
				<view class="menu-item" @click="handleAction('settings')">
					<view class="menu-icon-wrap" style="background-color: #fdba74;">
						<text class="iconfont menu-icon">&#xea8d;</text>
					</view>
					<text class="menu-label">{{ t('profile.menu.settings') }}</text>
					<text class="iconfont arrow-icon">&#xeb03;</text>
				</view>
			</view>

			<!-- 退出登录 -->
			<view class="logout-wrap">
				<button class="logout-btn" @click="handleLogout">
					<text class="logout-btn-text">退出登录</text>
				</button>
			</view>
		</scroll-view>
	</view>
</template>

<script setup lang="uts">
	import { useI18n } from '../../hooks/useI18n.uts'
	import { clearUserCache } from '../../store/user.uts'

	const { t } = useI18n()

	function handleAction(type : string) {
		uni.showToast({
			title: type + ' 功能开发中',
			icon: 'none'
		})
	}

	function handleLogout() {
		uni.showActionSheet({
			itemList: ['退出登录'],
			itemColor: '#F54A45',
			success: (res) => {
				if (res.tapIndex === 0) {
					clearUserCache()
				}
			}
		})
	}
</script>

<style>
	/* 页面基础样式 */
	.page {
		height: 100%;
		background-color: #F5F7FA;
		display: flex;
		flex-direction: column;
	}

	.status-bar {
		height: var(--status-bar-height);
		background-color: #3370FF;
	}

	.user-header {
		background-color: #3370FF;
		padding: 24px 20px 40px 20px;
	}

	.user-info-row {
		flex-direction: row;
		align-items: center;
	}

	.avatar {
		width: 60px;
		height: 60px;
		border-radius: 12px;
		background-color: #FFFFFF;
		border-width: 2px;
		border-color: rgba(255, 255, 255, 0.3);
	}

	.user-detail {
		flex: 1;
		margin-left: 16px;
	}

	.name-row {
		flex-direction: row;
		align-items: center;
	}

	.name {
		color: #FFFFFF;
		font-size: 20px;
		font-weight: 600;
	}

	.qr-icon {
		color: rgba(255, 255, 255, 0.7);
		font-size: 18px;
		margin-left: 8px;
	}

	.dept-row {
		margin-top: 12px;
	}

	.dept-text {
		color: rgba(255, 255, 255, 0.8);
		font-size: 13px;
	}

	.status-row {
		margin-top: 16px;
		background-color: rgba(255, 255, 255, 0.15);
		padding: 6px 12px;
		border-radius: 6px;
		align-self: flex-start;
	}

	.status-text {
		color: #FFFFFF;
		font-size: 12px;
	}

	.content {
		flex: 1;
		margin-top: -16px;
		height: 0;
		background-color: #F5F7FA;
	}

	.menu-group {
		background-color: #FFFFFF;
		border-radius: 16px 16px 0 0;
		padding: 8px 0;
	}

	.menu-item {
		flex-direction: row;
		align-items: center;
		padding: 14px 20px;
		background-color: #FFFFFF;
		position: relative;
	}

	.menu-item:active {
		background-color: #F2F3F5;
	}

	.menu-item-border {
		position: absolute;
		bottom: 0;
		left: 60px;
		right: 0;
		height: 0.5px;
		background-color: #EFF0F1;
	}

	.menu-icon-wrap {
		width: 28px;
		height: 28px;
		border-radius: 6px;
		justify-content: center;
		align-items: center;
		margin-right: 12px;
	}

	.menu-icon {
		color: #FFFFFF;
		font-size: 18px;
	}

	.menu-label {
		flex: 1;
		font-size: 16px;
		color: #1F2329;
		font-weight: 400;
	}

	.arrow-icon {
		color: #8F959E;
		font-size: 14px;
	}

	.logout-wrap {
		padding: 24px 20px;
	}

	.logout-btn {
		background-color: #FFFFFF;
		height: 48px;
		border-radius: 10px;
		justify-content: center;
		align-items: center;
		border-width: 0.5px;
		border-color: #EFF0F1;
	}

	.logout-btn:active {
		background-color: #FEEEEE;
	}

	.logout-btn-text {
		color: #F54A45;
		font-size: 16px;
		font-weight: 500;
	}
</style>