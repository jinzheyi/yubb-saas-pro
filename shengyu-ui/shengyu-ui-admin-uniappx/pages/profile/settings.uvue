<template>
	<view class="page" :style="{ backgroundColor: theme.pageBg }">
		<view class="status-bar" :style="{ backgroundColor: theme.headerBg }"></view>
		<view class="header" :style="{ backgroundColor: theme.headerBg, borderBottomColor: theme.borderColor }">
			<view class="header-left" @click.stop="handleBack">
				<text class="iconfont back-icon" :style="{ color: theme.headerText }">&#xeb04;</text>
				<text class="back-text" :style="{ color: theme.headerText }">返回</text>
			</view>
			<text class="header-title" :style="{ color: theme.headerText }">设置</text>
			<view class="header-right"></view>
		</view>

		<scroll-view class="content" scroll-y="true" show-scrollbar="false">
			<view class="settings-group" :style="{ backgroundColor: theme.cardBg }">
				<view class="list-item" hover-class="list-item-active" @click="handleAction('theme')">
					<text class="item-label" :style="{ color: theme.textPrimary }">切换主题</text>
					<text class="item-desc" :style="{ color: theme.textTertiary }">浅色 / 深色 / 跟随系统</text>
					<text class="iconfont arrow-icon" :style="{ color: theme.textTertiary }">&#xeb03;</text>
				</view>
				<view class="list-item item-border" hover-class="list-item-active" @click="handleAction('chatBubble')" :style="{ borderTopColor: theme.borderColor }">
					<text class="item-label" :style="{ color: theme.textPrimary }">聊天气泡</text>
					<text class="item-desc" :style="{ color: theme.textTertiary }">全局生效</text>
					<text class="iconfont arrow-icon" :style="{ color: theme.textTertiary }">&#xeb03;</text>
				</view>
				<view class="list-item item-border" hover-class="list-item-active" @click="handleAction('checkUpdate')" :style="{ borderTopColor: theme.borderColor }">
					<text class="item-label" :style="{ color: theme.textPrimary }">检查更新</text>
					<text class="item-desc" :style="{ color: theme.textTertiary }">当前版本 {{ appVersion }}</text>
					<text class="iconfont arrow-icon" :style="{ color: theme.textTertiary }">&#xeb03;</text>
				</view>
			</view>
		</scroll-view>
	</view>
</template>

<script setup lang="uts">
	import { onUnload } from '@dcloudio/uni-app'
	import { getAppVersion } from '../../utils/device.uts'
	import { getThemeTokens, subscribeThemeChange } from '../../store/theme.uts'
	import type { ThemeTokens } from '../../config/theme.uts'

	const appVersion = ref(getAppVersion())
	const theme = ref<ThemeTokens>(getThemeTokens())
	let unsubscribeTheme: (() => void) | null = null

	function handleBack() {
		uni.navigateBack()
	}

	function handleAction(type : string) {
		if (type === 'chatBubble') {
			uni.navigateTo({
				url: '/pages/message/chat-bubble'
			})
			return
		}
		if (type === 'theme') {
			uni.navigateTo({
				url: '/pages/profile/theme-settings'
			})
			return
		}
		if (type === 'checkUpdate') {
			uni.showToast({
				title: `当前版本 ${appVersion.value}`,
				icon: 'none'
			})
			return
		}
	}

	unsubscribeTheme = subscribeThemeChange((_, __, tokens) => {
		theme.value = tokens
	})

	onUnload(() => {
		if (unsubscribeTheme != null) {
			unsubscribeTheme()
			unsubscribeTheme = null
		}
	})
</script>

<style>
	.page {
		height: 100%;
		display: flex;
		flex-direction: column;
	}

	.status-bar {
		height: var(--status-bar-height);
	}

	.header {
		height: 44px;
		flex-direction: row;
		align-items: center;
		padding: 0 12px;
		justify-content: space-between;
		border-bottom-width: 0.5px;
	}

	.header-left {
		flex-direction: row;
		align-items: center;
		width: 80px;
	}

	.back-icon {
		font-size: 20px;
	}

	.back-text {
		font-size: 16px;
		margin-left: 4px;
	}

	.header-title {
		font-size: 17px;
		font-weight: 600;
	}

	.header-right {
		width: 80px;
	}

	.content {
		flex: 1;
		height: 0;
	}

	.settings-group {
		margin-top: 12px;
	}

	.list-item {
		height: 56px;
		flex-direction: row;
		align-items: center;
		padding: 0 16px;
	}

	.list-item-active {
		background-color: rgba(143, 149, 158, 0.12);
	}

	.item-border {
		border-top-width: 0.5px;
	}

	.item-label {
		flex: 1;
		font-size: 16px;
	}

	.item-desc {
		font-size: 13px;
		margin-right: 8px;
	}

	.arrow-icon {
		font-size: 14px;
	}
</style>
