/**
 * 角标管理 API
 */

import { request } from '../utils/request.uts'

/**
 * 获取角标数据
 * 从服务器获取用户的所有角标数据（总未读数、会话角标、菜单角标）
 */
export function getBadgeData(): Promise<any> {
  return request({
    url: '/system/im/badge/get',
    method: 'GET'
  })
}
